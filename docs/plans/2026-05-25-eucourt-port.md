# eu-court Port + Shared eu_member_states Resource — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Port the eu-court tracker (`old/eucourtstats.Rmd`) onto the euverse shared infra AND introduce `data-final/eu_member_states.parquet` — the canonical EU-membership master table that this port's eu-court page consumes and the next port (eu-law, Plan B) will also reuse.

**Architecture:** One shared cross-cutting resource (`data-final/eu_member_states.parquet`, built from a hand-curated `data-manual/eu-member-states.csv` via `R/build_eu_member_states.R`) + two new per-upstream-dataset fetchers (`R/fetch_curia_cases.R` for the Curia scraper, `R/fetch_eurlex_court_decisions.R` for the sector-6 SPARQL) + one new page (`trackers/eu-court.qmd`). No new R workflow file. All ggiraph plots use the shared `theme_mo()` + `register_ggiraph_defaults()`; tables use `dt_mo()`; the court palette uses `mo_palette$categorical[1:3]` and the choropleth uses `mo_palette$sequential` (viridis). Single commit + push triggers render+deploy.

**Tech Stack:** R 4.4.3 (renv 1.2.3); existing renv pin set + 6 new top-level packages: `eurlex` (the owner-maintained SPARQL client), `modelsummary` (forecast model table), `maps` (choropleth basemap), `forcats` (factor ordering), `purrr` (`map_chr` for title-fetches), `tidyr` (already transitive — making explicit). No new Python deps. Quarto 1.7.17.

**Spec reference:** `docs/specs/2026-05-25-eucourt-eulaw-port-design.md` (combined spec for both this plan and the follow-up eu-law plan).

**Conventions for this plan:**
- Working directory throughout: `C:\Users\uctqova\Documents\github\euverse`. Each command block re-asserts via `Set-Location`.
- Rscript not on PATH; use `& "C:\Program Files\R\R-4.4.3\bin\Rscript.exe"`.
- "Verify" steps run a command and compare to "Expected". Discrepancy = stop and surface.
- Commits happen only in Task 7 (single-commit plan).
- PowerShell `@"..."@` here-string-into-`Rscript -e` is unreliable; for multi-line R, write to a temp `.R` file, run via `Rscript <file>.R`, delete after.
- Eur-Lex SPARQL queries can take 30 s – several minutes; let them run rather than retry.
- The `eurlex` package's `elx_make_query()` returns a SPARQL string; chained with `elx_run_query()` to execute. Both must be loaded via `library(eurlex)`.

---

## Task 1: Install new renv packages + snapshot

**Files:**
- Modify: `renv.lock` (regenerated by snapshot)

- [ ] **Step 1: Install the 6 new packages**

```powershell
Set-Location "C:\Users\uctqova\Documents\github\euverse"
& "C:\Program Files\R\R-4.4.3\bin\Rscript.exe" -e "renv::install(c('eurlex','modelsummary','maps','forcats','purrr','tidyr'), prompt = FALSE)"
```

Expected: installation log shows packages installed. `eurlex` may pull `httr`/`xml2`/`rvest` (some already transitive). `modelsummary` pulls `kableExtra` and friends. `maps` is small. Cold install ~1–3 min. No "FAILED" lines.

- [ ] **Step 2: Snapshot with the scoped library**

```powershell
& "C:\Program Files\R\R-4.4.3\bin\Rscript.exe" -e "renv::snapshot(type = 'all', library = renv::paths$library(), prompt = FALSE)"
```

`library = renv::paths$library()` is mandatory — keeps the snapshot scoped to the project library (not the user's whole system R).

Expected: lockfile grows from ~275 KB → ~310–360 KB. Output begins "The following package(s) will be updated in the lockfile:" listing the 6 new packages + their transitive closure.

- [ ] **Step 3: Verify all 6 packages load**

```powershell
& "C:\Program Files\R\R-4.4.3\bin\Rscript.exe" -e "for (p in c('eurlex','modelsummary','maps','forcats','purrr','tidyr')) { suppressPackageStartupMessages(library(p, character.only = TRUE)); cat(p, packageVersion(p) |> as.character(), '\n') }"
```

Expected: 6 lines, each `<pkg> <version>`, no errors.

- [ ] **Step 4: Stage**

```powershell
git add renv.lock
```

---

## Task 2: Build the shared eu_member_states resource

**Files:**
- Create: `data-manual/eu-member-states.csv`
- Create: `R/build_eu_member_states.R`
- Generated: `data-final/eu_member_states.parquet` + `data-final/eu_member_states.meta.json`

- [ ] **Step 1: Author `data-manual/eu-member-states.csv`**

Use the `Write` tool. EXACT content (UTF-8, no BOM; comma-delimited; one row per Member State; 28 rows + 1 header):

```csv
country,iso2,accession_year,exit_year
Austria,AT,1995,
Belgium,BE,1952,
Bulgaria,BG,2007,
Croatia,HR,2013,
Cyprus,CY,2004,
Czechia,CZ,2004,
Denmark,DK,1973,
Estonia,EE,2004,
Finland,FI,1995,
France,FR,1952,
Germany,DE,1952,
Greece,GR,1981,
Hungary,HU,2004,
Ireland,IE,1973,
Italy,IT,1952,
Latvia,LV,2004,
Lithuania,LT,2004,
Luxembourg,LU,1952,
Malta,MT,2004,
Netherlands,NL,1952,
Poland,PL,2004,
Portugal,PT,1986,
Romania,RO,2007,
Slovakia,SK,2004,
Slovenia,SI,2004,
Spain,ES,1986,
Sweden,SE,1995,
United Kingdom,GB,1973,2020
```

(The empty trailing comma per row = empty `exit_year` for current members. UK is the only row with a populated `exit_year`. `read.csv` will parse empty → NA.)

- [ ] **Step 2: Author `R/build_eu_member_states.R`**

Use the `Write` tool. EXACT content:

```r
# build_eu_member_states.R
# Build the canonical EU Member States master table for the euverse site.
# Reads the hand-curated CSV at data-manual/eu-member-states.csv, validates,
# writes the parquet to data-final/eu_member_states.parquet + meta sidecar.
# Idempotent: re-running produces a byte-identical parquet.

suppressPackageStartupMessages({
  library(arrow)
  library(jsonlite)
  library(here)
})

src      <- "eu-member-states"
csv_path <- here::here("data-manual", "eu-member-states.csv")
out_pq   <- here::here("data-final", "eu_member_states.parquet")
out_meta <- here::here("data-final", "eu_member_states.meta.json")

# ---- 1. read + parse --------------------------------------------------------
if (!file.exists(csv_path)) {
  stop("Source CSV not found: ", csv_path, call. = FALSE)
}
df <- read.csv(csv_path, stringsAsFactors = FALSE, na.strings = "")
df$accession_year <- as.integer(df$accession_year)
df$exit_year      <- suppressWarnings(as.integer(df$exit_year))

# ---- 2. validate ------------------------------------------------------------
stopifnot(
  identical(names(df), c("country", "iso2", "accession_year", "exit_year")),
  nrow(df) >= 27L && nrow(df) <= 30L,
  !any(duplicated(df$iso2)),
  !any(duplicated(df$country)),
  all(nchar(df$iso2) == 2L),
  all(df$accession_year >= 1952L & df$accession_year <= 2050L),
  all(is.na(df$exit_year) | df$exit_year >= df$accession_year)
)

# ---- 3. write atomically ----------------------------------------------------
pq_tmp   <- tempfile(tmpdir = dirname(out_pq),   fileext = ".parquet")
meta_tmp <- tempfile(tmpdir = dirname(out_meta), fileext = ".json")

arrow::write_parquet(df, pq_tmp, compression = "zstd")
jsonlite::write_json(
  list(
    source          = src,
    built_at        = format(Sys.time(), "%Y-%m-%dT%H:%M:%SZ", tz = "UTC"),
    inputs          = "data-manual/eu-member-states.csv",
    builder_script  = "R/build_eu_member_states.R",
    rows            = nrow(df)
  ),
  meta_tmp, auto_unbox = TRUE, pretty = TRUE
)

file.rename(pq_tmp,   out_pq)
file.rename(meta_tmp, out_meta)
message("Built ", src, ": ", nrow(df), " rows -> ", out_pq)
```

- [ ] **Step 3: Run the build script**

```powershell
Set-Location "C:\Users\uctqova\Documents\github\euverse"
& "C:\Program Files\R\R-4.4.3\bin\Rscript.exe" R/build_eu_member_states.R
```

Expected: final line `Built eu-member-states: 28 rows -> .../data-final/eu_member_states.parquet`.

- [ ] **Step 4: Verify the parquet + meta**

```powershell
& "C:\Program Files\R\R-4.4.3\bin\Rscript.exe" -e "df <- arrow::read_parquet('data-final/eu_member_states.parquet'); cat('rows:', nrow(df), 'cols:', paste(names(df), collapse=','), '\n'); cat('founders (1952):', sum(df$accession_year == 1952), '\n'); cat('UK exit_year:', df$exit_year[df$iso2 == 'GB'], '\n'); cat('current members:', sum(is.na(df$exit_year)), '\n'); m <- jsonlite::fromJSON('data-final/eu_member_states.meta.json'); cat('meta source:', m$source, 'rows:', m$rows, '\n')"
```

Expected: rows=28, cols=`country,iso2,accession_year,exit_year`, founders=6, UK exit=2020, current=27, meta source `eu-member-states` rows=28.

- [ ] **Step 5: Stage**

```powershell
git add data-manual/eu-member-states.csv R/build_eu_member_states.R data-final/eu_member_states.parquet data-final/eu_member_states.meta.json
```

---

## Task 3: `R/fetch_curia_cases.R` (Curia scraper)

**Files:**
- Create: `R/fetch_curia_cases.R`
- Generated: `data-apis/curia_cases_latest.parquet` + `_latest.meta.json`

- [ ] **Step 1: Write the fetcher**

Use the `Write` tool. EXACT content:

```r
# fetch_curia_cases.R
# Source: Curia case list scraper (NOT SPARQL) via eurlex::elx_curia_list().
# Returns ~60-80K cases (all CJEU cases since 1953).
# Cleaning derived from old/eucourtstats.Rmd lines 169-222.

suppressPackageStartupMessages({
  library(arrow); library(jsonlite); library(here); library(digest)
  library(eurlex); library(dplyr); library(stringr)
})

src     <- "curia_cases"
out_dir <- here::here("data-apis")

# ---- 1. fetch ---------------------------------------------------------------
df <- tryCatch({
  raw <- elx_curia_list("all", parse = TRUE)

  cleaned <- raw |>
    mutate(see_case = case_when(
      str_detect(see_case, "[C|T|F] [:digit:]+/[:digit:]+") ~
        str_replace(see_case, "(?<=[C|T|F]) (?=[:digit:])", "-"),
      TRUE ~ see_case
    )) |>
    mutate(case_id = str_replace(case_id, "(?<=[:digit:])-(?=[:digit:])", "/")) |>
    filter(nchar(case_id) > 3) |>
    mutate(court = case_when(
      str_detect(case_id, "OPIN|C-|^[:digit:]|RULING") ~ "CJ",
      str_detect(case_id, "T-") ~ "GC",
      str_detect(case_id, "F-") ~ "CST",
      TRUE ~ NA_character_
    )) |>
    mutate(see_case_court = case_when(
      str_detect(see_case, "OPIN|C-|^[:digit:]|RULING") ~ "CJ",
      str_detect(see_case, "T-") ~ "GC",
      str_detect(see_case, "F-") ~ "CST",
      TRUE ~ NA_character_
    )) |>
    mutate(appeal_court = case_when(
      str_detect(appeal, "OPIN|C-|^[:digit:]|RULING") ~ "CJ",
      str_detect(appeal, "T-") ~ "GC",
      str_detect(appeal, "F-") ~ "CST",
      TRUE ~ NA_character_
    )) |>
    mutate(case_status = case_when(
      str_detect(case_id, "OPIN|RULING") ~ "Opinion",
      str_detect(case_info, "^Judge?ment") ~ "Judgment",
      str_detect(case_info, "^Order") ~ "Order",
      str_detect(case_info, "^Removed") ~ "Removal",
      str_detect(case_info, "^Pending") & is.na(ecli) ~ "Pending",
      str_detect(case_info, "^Seizure order") ~ "Seizure order",
      str_detect(case_info, "^Decision") & str_detect(case_id, "RX") ~ "Re-examination",
      str_detect(case_info, "^Third-party proce") ~ "Third-party proceedings",
      !is.na(see_case) & court != see_case_court ~ "Transferred",
      !is.na(see_case) & court == see_case_court ~ "Joined",
      TRUE ~ NA_character_
    )) |>
    mutate(case_year_raw = str_extract(case_id, "/[:digit:]{2}"),
           case_year_raw = str_remove(case_year_raw, "/"),
           case_year = case_when(
             str_detect(case_year_raw, "^0|^1|^2|^3") ~ as.integer(str_c("20", case_year_raw)),
             str_detect(case_year_raw, "^5|^6|^7|^8|^9") ~ as.integer(str_c("19", case_year_raw)),
             TRUE ~ NA_integer_
           ),
           case_number = as.integer(str_extract(case_id, "[:digit:]+(?=/)")),
           decision_year = as.integer(str_extract(ecli, "[:digit:]{4}")),
           decision_date_str = str_extract(
             case_info,
             "[:digit:]{1,2} (January|February|March|April|May|June|July|August|September|October|November|December) (19|20)[:digit:]{2}"
           )) |>
    select(case_id, ecli, court, see_case, see_case_court, appeal, appeal_court,
           case_status, case_year, case_number, decision_year, decision_date_str, case_info)

  as.data.frame(cleaned)
}, error = function(e) {
  message("Fetch failed: ", conditionMessage(e))
  quit(status = 1L)
})

# ---- 2. validate ------------------------------------------------------------
if (!is.data.frame(df) || nrow(df) == 0L) {
  message("Validation failed: empty or non-data.frame result")
  quit(status = 1L)
}
if (!all(c("case_id", "case_status", "court", "case_year") %in% names(df))) {
  message("Validation failed: missing required columns")
  quit(status = 1L)
}
if (!any(df$court == "CJ", na.rm = TRUE)) {
  message("Validation failed: no CJ cases in result")
  quit(status = 1L)
}

# ---- 3. write atomically ----------------------------------------------------
data_tmp <- tempfile(tmpdir = out_dir, fileext = ".parquet")
meta_tmp <- tempfile(tmpdir = out_dir, fileext = ".json")
arrow::write_parquet(df, data_tmp, compression = "zstd")
jsonlite::write_json(
  list(
    source      = src,
    fetched_at  = format(Sys.time(), "%Y-%m-%dT%H:%M:%SZ", tz = "UTC"),
    rows        = nrow(df),
    schema_hash = digest::digest(names(df), algo = "sha256")
  ),
  meta_tmp, auto_unbox = TRUE, pretty = TRUE
)
file.rename(data_tmp, file.path(out_dir, paste0(src, "_latest.parquet")))
file.rename(meta_tmp, file.path(out_dir, paste0(src, "_latest.meta.json")))
message("Wrote ", src, " latest snapshot: ", nrow(df), " rows.")
```

- [ ] **Step 2: Run the fetcher**

```powershell
& "C:\Program Files\R\R-4.4.3\bin\Rscript.exe" R/fetch_curia_cases.R
```

Expected: final line `Wrote curia_cases latest snapshot: <N> rows.` where N is in the tens of thousands (60K-80K). Network call to curia.europa.eu via `elx_curia_list` takes 1-3 min.

- [ ] **Step 3: Verify snapshot shape**

```powershell
& "C:\Program Files\R\R-4.4.3\bin\Rscript.exe" -e "df <- arrow::read_parquet('data-apis/curia_cases_latest.parquet'); cat('rows:', nrow(df), 'cols:', paste(names(df), collapse=','), '\n'); cat('courts:', paste(sort(unique(na.omit(df$court))), collapse=','), '\n'); cat('year range:', min(df$case_year, na.rm=TRUE), 'to', max(df$case_year, na.rm=TRUE), '\n'); cat('case_status counts:\n'); print(sort(table(df$case_status), decreasing=TRUE)[1:5])"
```

Expected: rows ~60K+; cols include `case_id,ecli,court,case_status,case_year,case_number,decision_year`; courts = `CJ,CST,GC` (sorted); year range 1953 to current year; top case_status types include "Judgment", "Order", "Pending".

- [ ] **Step 4: Stage**

```powershell
git add R/fetch_curia_cases.R data-apis/curia_cases_latest.parquet data-apis/curia_cases_latest.meta.json
```

---

## Task 4: `R/fetch_eurlex_court_decisions.R` (sector 6 SPARQL + title fetches)

**Files:**
- Create: `R/fetch_eurlex_court_decisions.R`
- Generated: `data-apis/eurlex_court_decisions_latest.parquet` + `_latest.meta.json`

This is the heaviest fetcher in the plan — SPARQL query against sector 6 returns 40–60K decision rows, then we do title fetches for ~50–200 rows with missing procedure info to infer their procedure class. Total runtime 5–15 min.

- [ ] **Step 1: Write the fetcher**

Use the `Write` tool. EXACT content:

```r
# fetch_eurlex_court_decisions.R
# Source: Eur-Lex SPARQL, sector 6 (court decisions). Plus title fetches
# for celexes with missing procedure metadata to infer procedure class.
# Cleaning derived from old/eucourtstats.Rmd lines 482-664.

suppressPackageStartupMessages({
  library(arrow); library(jsonlite); library(here); library(digest)
  library(eurlex); library(dplyr); library(tidyr); library(stringr); library(purrr)
})

src     <- "eurlex_court_decisions"
out_dir <- here::here("data-apis")

# ---- 1. fetch ---------------------------------------------------------------
df <- tryCatch({
  raw <- elx_make_query(
    "any", sector = 6,
    include_date = TRUE,
    include_court_procedure = TRUE,
    include_court_origin = TRUE,
    include_original_language = TRUE,
    include_court_formation = TRUE,
    include_judge_rapporteur = TRUE,
    include_ecli = TRUE
  ) |> elx_run_query()

  # ---- 2a. bring data to celex level --------------------------------------
  decisions <- raw |>
    mutate(clx_type  = str_sub(celex, 6, 7),
           clx_num   = as.integer(str_sub(celex, 8, 11)),
           clx_year  = as.integer(str_sub(celex, 2, 5)),
           clx_court = str_sub(celex, 6, 6),
           dec_type  = case_when(
             clx_type %in% c("CJ", "TJ", "FJ")             ~ "Judgment",
             clx_type %in% c("CO", "CB", "TO", "TB", "FO", "FB") ~ "Order",
             clx_type %in% c("CV", "CX")                   ~ "Opinion",
             TRUE ~ NA_character_
           )) |>
    filter(!is.na(dec_type), !str_detect(celex, "_INF|_SUM")) |>
    separate_wider_delim(cols = courtprocedure,
                         delim = " - ",
                         too_few = "align_start",
                         too_many = "merge",
                         cols_remove = TRUE,
                         names = c("procedure", "procedure_outcome")) |>
    mutate(procedure = ifelse(clx_type %in% c("FB", "FJ", "FO") & is.na(procedure),
                              "Staff case", procedure)) |>
    filter(!procedure %in% c("Rectification")) |>
    group_by(celex) |>
    reframe(
      decision          = str_c(unique(dec_type), collapse = "~~~"),
      ecli              = str_c(unique(ecli),     collapse = "~~~"),
      date              = str_c(unique(date),     collapse = "~~~"),
      procedure         = str_c(unique(procedure), collapse = "~~~"),
      procedure_outcome = str_c(unique(procedure_outcome), collapse = "~~~"),
      rapporteur        = str_c(unique(jr),       collapse = "~~~"),
      formation         = str_c(unique(cf),       collapse = "~~~"),
      language          = str_c(unique(origlang), collapse = "~~~"),
      origin            = str_c(unique(courtorigin), collapse = "~~~")
    ) |>
    mutate(across(everything(), ~str_squish(.)))

  # ---- 2b. dedupe CO/CB pairs (keep CO) -----------------------------------
  decisions <- decisions |>
    mutate(clx_type  = str_sub(celex, 6, 7),
           clx_num   = as.integer(str_sub(celex, 8, 11)),
           clx_year  = as.integer(str_sub(celex, 2, 5)),
           clx_court = str_sub(celex, 6, 6),
           clx_dec   = str_sub(celex, 7, 7)) |>
    group_by(clx_court, clx_year, clx_num) |>
    mutate(dupl = any(clx_dec %in% "B") & any(clx_dec %in% "O")) |>
    ungroup() |>
    filter(!(dupl == TRUE & clx_dec == "B")) |>
    select(-dupl)

  # ---- 2c. title fetches for missing procedures ---------------------------
  missing_procs <- decisions |>
    filter(is.na(procedure) | procedure == "" | procedure == "NA")
  message("Fetching titles for ", nrow(missing_procs), " missing-procedure cases...")

  missing_procs_titles <- missing_procs |>
    mutate(title = map_chr(
      str_c("http://publications.europa.eu/resource/celex/", celex),
      possibly(eurlex::elx_fetch_data, otherwise = NA_character_),
      "title"
    )) |>
    mutate(title = str_squish(title)) |>
    mutate(
      procedure = case_when(
        str_detect(title, "Rectification")                                   ~ "Rectification",
        str_detect(title, "Opinion of the Court|Request for an Opinion")     ~ "Request for an Opinion",
        str_detect(title, "DEP ")                                            ~ "Other",
        str_detect(celex, "FB") | str_detect(title, "[Ss]taff|[Cc]ivil [Ss]ervice") ~ "Staff case",
        str_detect(title, "reliminary referen|for a prelim|reliminary ruli") ~ "Preliminary reference",
        str_detect(title, "Commission( of the European Communities)? v (?!(Parliament|Council|European))") ~ "Failure to fulfil obligations",
        str_detect(celex, "FB") | str_detect(title, "[Aa]nnulment|Trademark|Trade Mark") ~ "Action for annulment",
        str_detect(celex, "FB") | str_detect(title, "damages")               ~ "Action for damages",
        str_detect(title, " (v|contre) (European Commission|Commission|Council|European Parliament|Parliament|EUIPO|OHIM|OHMI|ECB|Office for Har|European Union|Office de l'|EASO|EMA)") ~ "Action for annulment",
        TRUE ~ NA_character_
      ),
      origin = case_when(
        str_detect(title, "Czech Republic|Nejvyšš|Czechia") ~ "Czechia",
        str_detect(title, "Slovak Republic|Najvyšš|Prešov|Slovakia") ~ "Slovakia",
        str_detect(title, "Polish|Poland") ~ "Poland",
        str_detect(title, "Belgiqu|Hof van Cass|Brussel|Belgium") ~ "Belgium",
        str_detect(title, "Finland") ~ "Finland",
        str_detect(title, "Salzburg|Wien|Austria") ~ "Austria",
        str_detect(title, "Veliko Tarn|Varna|Bulgaria|Sofiys") ~ "Bulgaria",
        str_detect(title, "Juzgado|Audiencia Provincial|Tribunal Superior de Justicia|Spain") ~ "Spain",
        str_detect(title, "România|Romania") ~ "Romania",
        str_detect(title, "Rijeci|Croati|lučice") ~ "Croatia",
        str_detect(title, "Slovenia") ~ "Slovenia",
        str_detect(title, "Cyprus") ~ "Cyprus",
        str_detect(title, "Estonia") ~ "Estonia",
        str_detect(title, "Sweden") ~ "Sweden",
        str_detect(title, "Luxembourg") ~ "Luxembourg",
        str_detect(title, "Denmark|Dane?mark") ~ "Denmark",
        str_detect(title, "Hellenic|Greece|Simvoulio|Simboulio") ~ "Greece",
        str_detect(title, "Portugese|Tribunal da Relação|(?<!TAP )Portugal") ~ "Portugal",
        str_detect(title, "Lietuv|Vilniaus|Lithuania") ~ "Lithuania",
        str_detect(title, "Latvijas|Augstākās|Latvia") ~ "Latvia",
        str_detect(title, "Italia|Italy|Tribunale Amministrativ|Tribunale di|Consiglio di|Corte Su") ~ "Italy",
        str_detect(title, "Bíróság|Magyar|Budapest|Szeged|Hungary|Hongri") ~ "Hungary",
        str_detect(title, "Conseil d|(?<!Air )France") ~ "France",
        str_detect(title, "Irland|(?<!Northern )Ireland") ~ "Ireland",
        str_detect(title, "Netherlands|Den Haag|Raad van Ber|College van|Rechtbank Am|Raad [Vv]an St|Hoge Raad") ~ "Netherlands",
        str_detect(title, "England \\& Wales|England and Wales|Her Majest|United Kingdom") ~ "United Kingdom",
        str_detect(title, "Deutschland|Regensburg|Germany|Landgericht K|Amtsgericht") ~ "Germany",
        TRUE ~ NA_character_
      )
    )

  decisions <- decisions |>
    filter(!celex %in% missing_procs$celex) |>
    bind_rows(missing_procs_titles |> select(-title)) |>
    filter(!procedure %in% c("Rectification"))

  # ---- 2d. derive procedure_class, court_long, formation_simple ----------
  decisions |>
    mutate(procedure_class = case_when(
      str_detect(procedure, "eference.*preliminary|Preliminary reference") ~ "Preliminary rulings",
      str_detect(procedure, "Action for annulm")                           ~ "Annulment procedures",
      str_detect(procedure, "[Ff]ailure to fulf")                          ~ "Infringement proceedings",
      str_detect(procedure, "[Ss]taff")                                    ~ "Staff cases",
      TRUE                                                                 ~ "Other"
    )) |>
    mutate(court_long = case_when(
      clx_court == "C" ~ "Court of Justice",
      clx_court == "T" ~ "General Court",
      clx_court == "F" ~ "Civil Service Tribunal",
      TRUE             ~ NA_character_
    )) |>
    mutate(formation_simple = case_when(
      str_detect(formation, "Grand Chamber")                       ~ "Grand Chamber",
      str_detect(formation, "[Ff]ull [Cc]ourt")                    ~ "Full Court",
      str_detect(formation, "Single|Vice-President|President|Judge hearing") ~ "Single Judge",
      TRUE                                                         ~ "Panel"
    )) |>
    mutate(rapporteur = case_when(
      str_detect(rapporteur, "O.Higgins") ~ "OHiggins",
      str_detect(rapporteur, "O'Keeffe")  ~ "OKeeffe",
      str_detect(rapporteur, "Cz.cz")     ~ "Czucz",
      str_detect(rapporteur, ".an .er Woude") ~ "van der Woude",
      TRUE                                ~ rapporteur
    )) |>
    # static celex-specific corrections (from old Rmd lines 642-664)
    filter(!celex %in% c("62019CO0519")) |>
    mutate(
      origin = case_when(
        celex == "62019CJ0550" ~ "Spain",
        celex == "62020CJ0339" ~ "France",
        celex == "62014CB0196" ~ "Germany",
        celex == "62022CB0578" ~ "Germany",
        celex == "62023CB0052" ~ "Germany",
        TRUE                   ~ origin
      ),
      language = case_when(
        celex == "62017CO0707" ~ "Bulgarian",
        celex == "62020CJ0306" ~ "Latvian",
        celex == "62017CJ0122" ~ "English",
        celex == "62018CJ0276" ~ "Hungarian",
        celex == "62013CJ0512" ~ "Dutch",
        celex == "62022CJ0142" ~ "English",
        celex == "61987CJ0245" ~ "German",
        celex == "62000CJ0167" ~ "German",
        celex == "62012CJ0293" ~ "English~~~German",
        TRUE                   ~ language
      )
    ) |>
    select(celex, clx_court, clx_year, date, decision, procedure, procedure_class,
           procedure_outcome, formation_simple, rapporteur, language, origin, ecli) |>
    as.data.frame()

}, error = function(e) {
  message("Fetch failed: ", conditionMessage(e))
  quit(status = 1L)
})

# ---- 3. validate ------------------------------------------------------------
if (!is.data.frame(df) || nrow(df) < 10000L) {
  message("Validation failed: too few rows (", nrow(df), ")")
  quit(status = 1L)
}
required_cols <- c("celex", "clx_court", "decision", "procedure_class", "date")
if (!all(required_cols %in% names(df))) {
  message("Validation failed: missing columns ",
          paste(setdiff(required_cols, names(df)), collapse = ", "))
  quit(status = 1L)
}

# ---- 4. write atomically ----------------------------------------------------
data_tmp <- tempfile(tmpdir = out_dir, fileext = ".parquet")
meta_tmp <- tempfile(tmpdir = out_dir, fileext = ".json")
arrow::write_parquet(df, data_tmp, compression = "zstd")
jsonlite::write_json(
  list(
    source      = src,
    fetched_at  = format(Sys.time(), "%Y-%m-%dT%H:%M:%SZ", tz = "UTC"),
    rows        = nrow(df),
    schema_hash = digest::digest(names(df), algo = "sha256")
  ),
  meta_tmp, auto_unbox = TRUE, pretty = TRUE
)
file.rename(data_tmp, file.path(out_dir, paste0(src, "_latest.parquet")))
file.rename(meta_tmp, file.path(out_dir, paste0(src, "_latest.meta.json")))
message("Wrote ", src, " latest snapshot: ", nrow(df), " rows.")
```

- [ ] **Step 2: Run the fetcher (5–15 min)**

```powershell
& "C:\Program Files\R\R-4.4.3\bin\Rscript.exe" R/fetch_eurlex_court_decisions.R
```

Expected: progress message `Fetching titles for <N> missing-procedure cases...` (N typically 50–500). Final line `Wrote eurlex_court_decisions latest snapshot: <M> rows.` (M ~ 40K–60K). SPARQL takes 2–5 min; title fetches take 1–5 min depending on N.

- [ ] **Step 3: Verify snapshot shape**

```powershell
& "C:\Program Files\R\R-4.4.3\bin\Rscript.exe" -e "df <- arrow::read_parquet('data-apis/eurlex_court_decisions_latest.parquet'); cat('rows:', nrow(df), 'cols:', paste(names(df), collapse=','), '\n'); cat('clx_courts:', paste(sort(unique(df$clx_court)), collapse=','), '\n'); cat('decisions:', paste(sort(unique(df$decision)), collapse=','), '\n'); cat('procedure_classes:\n'); print(sort(table(df$procedure_class), decreasing=TRUE))"
```

Expected: rows ~40K-60K; cols include `celex,clx_court,date,decision,procedure_class,formation_simple,rapporteur,origin,ecli`; clx_courts = `C,F,T`; decisions includes `Judgment`, `Order`, `Opinion`; procedure_classes includes `Annulment procedures`, `Infringement proceedings`, `Preliminary rulings`, `Staff cases`, `Other`.

- [ ] **Step 4: Stage**

```powershell
git add R/fetch_eurlex_court_decisions.R data-apis/eurlex_court_decisions_latest.parquet data-apis/eurlex_court_decisions_latest.meta.json
```

---

## Task 5: Create `trackers/eu-court.qmd`

**Files:**
- Create: `trackers/eu-court.qmd`

This is the largest file in the plan — 7 sections, 6 ggiraph plots, forecast model with modelsummary, choropleth. Single `Write`. Setup chunk loads all 3 snapshots (`curia_cases`, `eurlex_court_decisions`, `eu_member_states`).

- [ ] **Step 1: Write the page**

Use the `Write` tool to create `trackers/eu-court.qmd`. EXACT content (the OUTER 4-backtick fences are JUST for prompt display — the FILE uses 3-backtick R chunks):

````markdown
---
title: "Judicial Proceedings in the European Union"
subtitle: "An automatically updated overview of the EU's judicial activity"
toc: true
---

```{r setup, include=FALSE}
suppressPackageStartupMessages({
  library(dplyr)
  library(tidyr)
  library(stringr)
  library(forcats)
  library(purrr)
  library(ggplot2)
  library(ggiraph)
  library(here)
})
source(here::here("R", "theme.R"))
source(here::here("R", "freshness.R"))
source(here::here("R", "table.R"))
register_ggiraph_defaults()

# Load snapshots once at the top.
cc  <- read_with_freshness("curia_cases")
ed  <- read_with_freshness("eurlex_court_decisions")
ms  <- arrow::read_parquet(here::here("data-final", "eu_member_states.parquet"))

today_d  <- Sys.Date()
year_now <- as.integer(format(today_d, "%Y"))

curia_cases <- cc$data
decisions   <- ed$data

# Year × Member State panel from the shared master.
ms_panel <- tibble(year = 1952:year_now) |>
  tidyr::crossing(ms) |>
  filter(year >= accession_year,
         is.na(exit_year) | year < exit_year) |>
  select(year, country, iso2)

# Yearly count of MS (for prelim-refs context — not currently displayed but
# kept for parity with the eu-law port's reuse pattern).
n_ms_by_year <- ms_panel |> count(year, name = "n_ms")

# Inline-stats: caseload totals.
n_cases       <- curia_cases |> filter(case_status != "Transferred" | is.na(case_status)) |> summarise(n = n_distinct(case_id)) |> pull(n)
n_decisions   <- curia_cases |> filter(!is.na(ecli)) |> summarise(n = n_distinct(ecli)) |> pull(n)
n_pending_cj  <- curia_cases |> filter(case_status == "Pending", court == "CJ") |> summarise(n = n_distinct(case_id)) |> pull(n)
n_pending_gc  <- curia_cases |> filter(case_status == "Pending", court == "GC") |> summarise(n = n_distinct(case_id)) |> pull(n)

# Court palette mapped to mo_palette$categorical[1:3] (replaces old bespoke pink/navy/teal).
court_palette <- tibble(
  court      = factor(c("CJ", "GC", "CST"), levels = c("CJ", "GC", "CST")),
  court_long = factor(c("Court of Justice", "General Court", "Civil Service Tribunal"),
                      levels = c("Court of Justice", "General Court", "Civil Service Tribunal")),
  color      = mo_palette$categorical[1:3]
)
```

The Court of Justice of the European Union (CJEU) is the principal
judicial institution of the EU. It consists of two courts: the
higher-instance Court of Justice (CJ) and the lower-instance General
Court (GC). There used to be a third court, the Civil Service Tribunal
(CST), which was abolished and whose jurisdiction was transferred to
the GC.

This page gives a real-time overview of the EU courts' output and
engagement with national courts through the preliminary ruling
procedure. It relies on data collected via the
[eurlex](https://github.com/michalovadek/eurlex) package for R.^[Because
neither [Eur-Lex](https://eur-lex.europa.eu/homepage.html) nor
[Curia](https://curia.europa.eu/juris/) contains the complete record of
all court proceedings and documents, the data below should be viewed as
a reasonable approximation of the true state of the world. The
[IUROPA](https://iuropa.pol.gu.se/) project is attempting to create
more comprehensive datasets.]

As of `r format(today_d, "%d %B %Y")`, EU courts have delivered
`r format(n_decisions, big.mark = ",")` decisions in about
`r format(n_cases, big.mark = ",")` cases submitted to them since 1953.
According to public records, **`r n_pending_cj`** cases are currently
pending before the Court of Justice and **`r n_pending_gc`** before the
General Court.

```{r recent-cj-cases, echo=FALSE}
curia_cases |>
  filter(court == "CJ") |>
  arrange(desc(case_year), desc(case_number)) |>
  slice(1:50) |>
  transmute(
    `Case ID` = case_id,
    Info      = case_info
  ) |>
  dt_mo()
```

# Number of cases

Even more than any other branch of the EU, the CJEU's caseload changed
dramatically over the course of European integration. We can visualise
the annual caseload of the three EU courts since 1953.

```{r ncasesyearcourt, echo=FALSE, fig.height=6, fig.width=9}
n_cases_year_court <- curia_cases |>
  filter(!is.na(court), !is.na(case_year)) |>
  summarise(n = n_distinct(case_id), .by = c(court, case_year)) |>
  right_join(tidyr::expand(curia_cases |> filter(!is.na(court), !is.na(case_year)),
                           court, case_year), by = c("court", "case_year")) |>
  mutate(n = ifelse(is.na(n), 0L, n)) |>
  left_join(court_palette, by = "court") |>
  group_by(court) |>
  mutate(global_mean = mean(n)) |>
  ungroup() |>
  mutate(tooltip = paste0("N = ", n, " (", case_year, ")"),
         data_id = paste0(court, case_year))

p_caseload <- ggplot(n_cases_year_court,
                     aes(x = case_year, y = n, fill = color)) +
  geom_hline(aes(yintercept = global_mean, colour = color),
             lty = 2, alpha = 0.4, linewidth = 0.4) +
  geom_col_interactive(aes(tooltip = tooltip, data_id = data_id),
                       colour = "grey96") +
  scale_fill_identity() +
  scale_colour_identity() +
  scale_x_continuous(breaks = seq(1950, year_now + 1, by = 10)) +
  facet_wrap(~ court_long, scales = "free_y", dir = "v") +
  labs(x = NULL, y = NULL,
       title = "Number of cases received by EU courts",
       subtitle = "Aggregated by year and court",
       caption = "Dashed line shows the mean") +
  theme_mo() +
  theme(strip.text = element_text(hjust = 0, face = "bold"))

girafe(ggobj = p_caseload, width_svg = 9, height_svg = 6)
```

The increase in the CJ's and GC's caseloads was mostly gradual.
Interestingly, the outsourcing of the civil service agenda to the CST
did not prevent the GC's caseload from rising during its period of
operation.

# Forecasting

As most other organisations, EU courts operate under a budget
constraint while facing some uncertainty about their future workload.
With information on the number of cases received in the past in hand,
we can build a simple autoregressive model that predicts this year's
and next year's CJ caseload.

The model uses lags 1, 2 and 3 of CJ caseload plus a 3-year lag of GC
caseload (since CJ caseload is linked to GC's through appeal
procedures).

```{r forecast, echo=FALSE, fig.height=4, fig.width=9}
dat_reg <- n_cases_year_court |>
  filter(case_year > 1994, court != "CST") |>
  arrange(case_year) |>
  group_by(court) |>
  mutate(lag1 = lag(n), lag2 = lag(n, 2), lag3 = lag(n, 3)) |>
  ungroup() |>
  filter(case_year > 2000) |>
  select(court, case_year, n, lag1, lag2, lag3) |>
  tidyr::pivot_wider(id_cols = case_year, names_from = court,
                     values_from = c(n, lag1, lag2, lag3))

out_ols <- lm(data = dat_reg |> filter(case_year < year_now),
              formula = n_CJ ~ lag1_CJ + lag2_CJ + lag3_CJ + lag3_GC)
out_poi <- glm(data = dat_reg |> filter(case_year < year_now),
               family = poisson(),
               formula = n_CJ ~ lag1_CJ + lag2_CJ + lag3_CJ + lag3_GC)

yhat_ols <- as.integer(round(predict(out_ols, dat_reg[nrow(dat_reg), ], type = "response"), 0))
yhat_poi <- as.integer(round(predict(out_poi, dat_reg[nrow(dat_reg), ], type = "response"), 0))

# t+1 prediction (use averaged prediction as new lag1_CJ)
newdat_t1 <- tibble(
  case_year = year_now + 1,
  lag1_CJ   = (yhat_ols + yhat_poi) / 2,
  lag2_CJ   = dat_reg$lag1_CJ[nrow(dat_reg)],
  lag3_CJ   = dat_reg$lag2_CJ[nrow(dat_reg)],
  lag3_GC   = dat_reg$lag2_GC[nrow(dat_reg)]
)
yhat_ols_t1 <- as.integer(round(predict(out_ols, newdat_t1, type = "response"), 0))

# Plot: actual bars + 2 dashed-outline prediction bars
plot_dat <- n_cases_year_court |>
  filter(case_year > year_now - 7, court == "CJ") |>
  mutate(tooltip = paste0("N = ", n), data_id = paste0("actual", case_year))

p_forecast <- ggplot(plot_dat, aes(x = case_year, y = n)) +
  geom_col(data = tibble(case_year = year_now, n = yhat_ols),
           colour = court_palette$color[court_palette$court == "CJ"],
           fill   = court_palette$color[court_palette$court == "CJ"],
           alpha = 0.15, lty = 2) +
  geom_col_interactive(
    data = tibble(case_year = year_now + 1, n = yhat_ols_t1,
                  tooltip = paste0("Predicted N = ", yhat_ols_t1),
                  data_id = paste0("pred", year_now + 1)),
    aes(tooltip = tooltip, data_id = data_id),
    colour = court_palette$color[court_palette$court == "CJ"],
    fill   = court_palette$color[court_palette$court == "CJ"],
    alpha = 0.15, lty = 2
  ) +
  geom_col_interactive(aes(fill = color, tooltip = tooltip, data_id = data_id)) +
  scale_fill_identity() +
  scale_x_continuous(breaks = (year_now - 7):(year_now + 1)) +
  labs(x = NULL, y = NULL,
       title = "Number of cases received by the Court of Justice",
       subtitle = "Forecast for the current and next year",
       caption = "Predictions shown as dashed-outline bars") +
  theme_mo()

girafe(ggobj = p_forecast, width_svg = 9, height_svg = 4)
```

```{r forecast-table, echo=FALSE}
suppressPackageStartupMessages(library(modelsummary))
modelsummary::modelsummary(
  list("OLS" = out_ols, "Poisson" = out_poi),
  statistic  = "conf.int",
  conf_level = 0.95,
  stars      = TRUE,
  title      = "Autoregressive model of CJ yearly caseload"
)
```

Both Gaussian and Poisson regression yield comparable predictions for
`r year_now` (`r yhat_ols` versus `r yhat_poi` cases).

# Decisions

The number of cases received is not identical to the number of judicial
decisions produced. Some cases are withdrawn or otherwise discontinued,
others are joined and decided together. The EU judicial system
distinguishes between different types of public decisions, of which
judgments and orders are the two most common.

```{r decisiontype, echo=FALSE, fig.height=5, fig.width=9}
n_dec_type_proc <- decisions |>
  filter(decision != "Opinion",
         procedure_class != "Other",
         clx_court == "C") |>
  mutate(decision_year = as.integer(str_sub(date, 1, 4))) |>
  count(decision_year, decision, procedure_class) |>
  tidyr::drop_na() |>
  mutate(tooltip = paste0(decision, "s = ", n, " (", decision_year, ")"),
         data_id = paste0("C", decision_year, procedure_class, decision))

p_dec_type <- ggplot(n_dec_type_proc,
                     aes(x = decision_year, y = n, fill = decision)) +
  geom_col_interactive(aes(tooltip = tooltip, data_id = data_id),
                       colour = "grey96", show.legend = TRUE) +
  scale_fill_manual(values = mo_palette$categorical[c(1, 2)]) +
  scale_x_continuous(breaks = seq(1950, year_now + 1, by = 10)) +
  facet_wrap(~ fct_infreq(procedure_class), dir = "v", scales = "free_y") +
  labs(x = NULL, y = NULL,
       title = "Decisions handed down by the Court of Justice",
       subtitle = "By year and procedure type",
       fill = NULL) +
  theme_mo() +
  theme(strip.text = element_text(hjust = 0, face = "bold"))

girafe(ggobj = p_dec_type, width_svg = 9, height_svg = 5)
```

The four main procedure types are preliminary rulings (Art 267 TFEU),
annulment procedures (Art 263), infringement proceedings (Art 258) and
staff cases. The CJ has been handing down a seemingly ever-increasing
number of preliminary rulings on questions from national courts.

```{r formationproportion, echo=FALSE, fig.height=4, fig.width=9}
form_dat <- decisions |>
  filter(clx_court == "C") |>
  mutate(decision_year = as.integer(str_sub(date, 1, 4))) |>
  count(decision_year, formation_simple) |>
  filter(decision_year > 2004) |>
  group_by(decision_year) |>
  mutate(prop = n / sum(n)) |>
  ungroup() |>
  mutate(formation_simple = factor(formation_simple,
                                   levels = c("Full Court", "Grand Chamber", "Panel", "Single Judge")),
         tooltip = paste0(formation_simple, " = ", round(prop, 3) * 100, "% (", decision_year, ")"),
         data_id = paste0(decision_year, formation_simple))

p_formation <- ggplot(form_dat,
                      aes(x = decision_year, y = prop, fill = formation_simple)) +
  geom_col_interactive(aes(tooltip = tooltip, data_id = data_id),
                       colour = "grey96", show.legend = TRUE) +
  scale_y_continuous(labels = scales::percent) +
  scale_fill_manual(values = mo_palette$categorical[c(1, 2, 3, 4)]) +
  scale_x_continuous(breaks = seq(2005, year_now + 1, by = 3)) +
  labs(x = NULL, y = NULL,
       title = "Proportion of CJ decisions by formation",
       fill = NULL) +
  theme_mo()

girafe(ggobj = p_formation, width_svg = 9, height_svg = 4)
```

Decisions can be further classified by the deciding court formation.
The most important cases at the CJ are decided by more than ten judges
in the Grand Chamber; on special occasions, the CJ even sits as the
Full Court. Most cases are decided by a panel of five judges.

```{r rapporteurs, echo=FALSE, fig.height=6, fig.width=9}
top_rapps <- decisions |>
  filter(clx_court %in% c("C", "T"),
         procedure_class != "Other",
         decision == "Judgment",
         !is.na(rapporteur), rapporteur != "") |>
  mutate(court_long = ifelse(clx_court == "C", "Court of Justice", "General Court")) |>
  count(court_long, rapporteur) |>
  group_by(court_long) |>
  slice_max(n, n = 20) |>
  ungroup()

rapp_dat <- decisions |>
  filter(clx_court %in% c("C", "T"),
         procedure_class != "Other",
         decision == "Judgment",
         !is.na(rapporteur), rapporteur != "") |>
  mutate(court_long = ifelse(clx_court == "C", "Court of Justice", "General Court")) |>
  semi_join(top_rapps, by = c("court_long", "rapporteur")) |>
  count(court_long, rapporteur, procedure_class) |>
  mutate(sum = sum(n), .by = c(court_long, rapporteur),
         tooltip = paste0(procedure_class, " = ", n, " (", rapporteur, ")"),
         data_id = paste0(court_long, rapporteur, procedure_class))

p_rapp <- ggplot(rapp_dat,
                 aes(x = n, y = reorder(rapporteur, sum), fill = procedure_class)) +
  geom_col_interactive(aes(tooltip = tooltip, data_id = data_id),
                       colour = "grey96", position = "stack", show.legend = TRUE) +
  scale_fill_manual(values = mo_palette$categorical[c(1, 2, 5, 3)]) +
  facet_wrap(~ court_long, dir = "v", scales = "free") +
  labs(x = NULL, y = NULL,
       title = "Most frequent judge-rapporteurs",
       subtitle = "Judgments in the four main procedure types only",
       fill = NULL) +
  theme_mo() +
  theme(strip.text = element_text(hjust = 0, face = "bold"))

girafe(ggobj = p_rapp, width_svg = 9, height_svg = 6)
```

Each decision is assigned a judge-rapporteur. This judge has the
primary responsibility for drafting the decision. The main predictor of
how many decisions a judge writes is the length of their tenure.

# Preliminary references

Historically the most important subset of CJEU cases comes through the
preliminary ruling procedure (Art 267 TFEU). The most well-known CJ
decisions — _Costa v ENEL_, _Cassis de Dijon_ — originated in references
from national courts.

```{r prelrefsmap, echo=FALSE, fig.height=6, fig.width=9}
references <- decisions |>
  filter(str_detect(procedure_class, "Preliminary")) |>
  tidyr::separate_rows(origin, sep = "~~~") |>
  filter(!is.na(origin), origin != "")

n_refs_country <- references |>
  arrange(date) |>
  group_by(origin) |>
  summarise(year_first = min(clx_year, na.rm = TRUE),
            n_refs     = n(), .groups = "drop") |>
  mutate(tooltip = paste0(origin, ": ", n_refs, " references. First case referred in ", year_first, "."))

# maps::map_data + Czech Republic / UK name normalisation
refs_map_dat <- maps::map_data("world",
                               xlim = c(-10.66, 51.55),
                               ylim = c(34.5, 71.05)) |>
  mutate(region = ifelse(region == "Czech Republic", "Czechia", region),
         region = ifelse(region == "UK", "United Kingdom", region))

p_map <- ggplot(n_refs_country) +
  geom_map_interactive(
    aes(map_id = origin, fill = n_refs, tooltip = tooltip, data_id = origin),
    map = refs_map_dat, colour = "grey28", size = 0.2, show.legend = FALSE
  ) +
  expand_limits(x = c(-13.66, 38.55), y = c(34.5, 71.05)) +
  scale_fill_gradientn(colours = mo_palette$sequential(9)) +
  labs(x = NULL, y = NULL,
       title = "Preliminary references by country of origin",
       subtitle = "Total number of closed cases referred by national courts") +
  theme_mo() +
  theme(panel.grid = element_blank(),
        axis.text  = element_blank())

girafe(ggobj = p_map, width_svg = 9, height_svg = 6)
```

References from national courts are very unevenly distributed
geographically. By far the highest number of references has come from
Germany. There is a relationship between length of EU membership and
total references.

```{r prelreftime, echo=FALSE, fig.height=7, fig.width=9}
# Year × country panel of references with zeros filled in.
ms_active_years <- ms_panel |> rename(origin = country)
n_ref_year <- references |>
  rename(year = clx_year) |>
  group_by(origin, year) |>
  summarise(n_refs = n(), .groups = "drop")

n_ref_year_all <- ms_active_years |>
  filter(year > 1960) |>
  left_join(n_ref_year, by = c("origin", "year")) |>
  mutate(n_refs = ifelse(is.na(n_refs), 0L, n_refs),
         tooltip = paste0("N = ", n_refs, " (", year, ")"),
         data_id = paste0(origin, year))

p_refs_time <- ggplot(n_ref_year_all, aes(x = year, y = n_refs)) +
  geom_point_interactive(aes(tooltip = tooltip, data_id = data_id),
                         colour = mo_palette$categorical[2], alpha = 0.4, size = 0.9) +
  geom_smooth(colour = mo_palette$categorical[6], fill = mo_palette$categorical[8],
              alpha = 0.15, linewidth = 0.6) +
  scale_y_continuous(limits = c(0, NA),
                     breaks = function(x) unique(floor(pretty(seq(0, (max(x) + 1) * 1.1))))) +
  scale_x_continuous(breaks = scales::pretty_breaks(3)) +
  facet_wrap(~ origin, scales = "free") +
  labs(x = NULL, y = NULL,
       title = "Preliminary references over time",
       subtitle = "By country of origin (free y-scale per panel)") +
  theme_mo() +
  theme(strip.text = element_text(hjust = 0, face = "bold", size = 6.5),
        axis.text  = element_text(size = 5))

girafe(ggobj = p_refs_time, width_svg = 9, height_svg = 7)
```

Referral activity has increased over time in most countries as the CJEU
and its preliminary-ruling procedure gained prominence. Note that the
data here comes from _closed_ cases only — many recent referrals are
still pending.

# About this page

Source data:

- [`R/fetch_curia_cases.R`](https://github.com/michalovadek/euverse/blob/main/R/fetch_curia_cases.R)
  scrapes the Curia case list via the `eurlex` package; snapshot at
  [`data-apis/curia_cases_latest.parquet`](https://github.com/michalovadek/euverse/blob/main/data-apis/curia_cases_latest.parquet).
- [`R/fetch_eurlex_court_decisions.R`](https://github.com/michalovadek/euverse/blob/main/R/fetch_eurlex_court_decisions.R)
  queries Eur-Lex's SPARQL endpoint (sector 6); snapshot at
  [`data-apis/eurlex_court_decisions_latest.parquet`](https://github.com/michalovadek/euverse/blob/main/data-apis/eurlex_court_decisions_latest.parquet).
- EU Member State years come from
  [`data-final/eu_member_states.parquet`](https://github.com/michalovadek/euverse/blob/main/data-final/eu_member_states.parquet)
  (built from `data-manual/eu-member-states.csv`).

The most recent case received by the CJ is
**`r format(max(curia_cases$case_year[curia_cases$court == 'CJ'], na.rm = TRUE), big.mark = '')`**.

Cite as: Ovádek, M. *Judicial Proceedings in the European Union*. Accessed
`r format(Sys.Date(), "%d %B %Y")`. Available at
<https://michalovadek.github.io/euverse/trackers/eu-court.html>.
````

- [ ] **Step 2: Verify chunk count + size**

```powershell
$qmd = Get-Content -Raw trackers/eu-court.qmd
$chunks = ([regex]'(?m)^```\{r ').Matches($qmd).Count
"r chunks: $chunks"
"file size: $((Get-Item trackers/eu-court.qmd).Length) bytes"
```

Expected: chunks ≥ 9 (setup, recent-cj-cases, ncasesyearcourt, forecast, forecast-table, decisiontype, formationproportion, rapporteurs, prelrefsmap, prelreftime — 10 total). File size ~12–20 KB.

- [ ] **Step 3: Stage**

```powershell
git add trackers/eu-court.qmd
```

---

## Task 6: Navbar + AGENTS.md + local smoke render

**Files:**
- Modify: `_quarto.yml` (add EU Court entry)
- Modify: `AGENTS.md` (mark eu-court row done)
- Generated: `_site/trackers/eu-court.html` etc.

- [ ] **Step 1: Add EU Court to the navbar**

Use the `Edit` tool on `_quarto.yml`. Replace:

```yaml
      - text: Trackers
        menu:
          - href: trackers/eu-finance.qmd
            text: EU Finance
          - href: trackers/eu-vetoes.qmd
            text: EU Vetoes
```

With:

```yaml
      - text: Trackers
        menu:
          - href: trackers/eu-finance.qmd
            text: EU Finance
          - href: trackers/eu-vetoes.qmd
            text: EU Vetoes
          - href: trackers/eu-court.qmd
            text: EU Court
```

- [ ] **Step 2: Verify YAML parses**

```bash
cd "C:/Users/uctqova/Documents/github/euverse" && uv run --with pyyaml python -c "import yaml; d=yaml.safe_load(open('_quarto.yml')); m=d['website']['navbar']['left'][1]['menu']; print('entries:', [(e['text'], e['href']) for e in m])"
```

Expected: 3 entries — `[('EU Finance', '...'), ('EU Vetoes', '...'), ('EU Court', 'trackers/eu-court.qmd')]`.

- [ ] **Step 3: Mark AGENTS.md migration map row done**

Use the `Edit` tool on `AGENTS.md`. Replace:

```
| `old/eucourtstats.Rmd`                             | Rmd + nightly cron | `trackers/eu-court.qmd`   | `data-apis/eurlex_*`         |
```

With:

```
| `old/eucourtstats.Rmd` ✅ ported 2026-05-25        | Rmd + nightly cron | `trackers/eu-court.qmd`   | `data-apis/eurlex_*`         |
```

- [ ] **Step 4: Local smoke render**

```powershell
Set-Location "C:\Users\uctqova\Documents\github\euverse"
quarto render 2>&1 | Select-String -Pattern "Output created|Error|ERROR|WARN" | Out-String
```

Expected: AT MINIMUM `Output created: _site\trackers\eu-court.html`. Other pages may be cache-skipped. Any `Error`/`ERROR` is STOP-the-line — report BLOCKED with the error and a 5-line excerpt. The Windows temp-cleanup `os error 32` line at the end is benign.

- [ ] **Step 5: Verify render markers**

```powershell
$html = Get-Content -Raw _site/trackers/eu-court.html
$checks = @(
  @{ name = "page title";              pattern = "Judicial Proceedings" },
  @{ name = "intro footnote anchor";   pattern = "fnref|footnote-ref" },
  @{ name = "recent-cases DT";         pattern = "dataTables_wrapper|datatable" },
  @{ name = "Section: cases by court"; pattern = "Number of cases" },
  @{ name = "Section: forecasting";    pattern = "Forecasting" },
  @{ name = "Section: decisions";      pattern = "^# Decisions|<h1[^>]*>Decisions" },
  @{ name = "Section: prelim refs";    pattern = "Preliminary references" },
  @{ name = "ggiraph SVG present";     pattern = "<svg" },
  @{ name = "Okabe-Ito #E69F00";       pattern = "#E69F00" },
  @{ name = "Okabe-Ito #56B4E9";       pattern = "#56B4E9" },
  @{ name = "modelsummary table";      pattern = "OLS|Poisson" },
  @{ name = "choropleth map_id attr";  pattern = "map_id|geom_map" },
  @{ name = "citation URL";            pattern = "michalovadek.github.io/euverse/trackers/eu-court.html" }
)
foreach ($c in $checks) {
  if ($html -match $c.pattern) { "PASS: $($c.name)" } else { "FAIL: $($c.name)" }
}
```

Expected: 13 PASS, 0 FAIL. If any FAIL, stop and report.

- [ ] **Step 6: Verify navbar updated in home.html**

```powershell
$home = Get-Content -Raw _site/home.html
if ($home -notmatch "EU Court") {
  "INFO: re-rendering home.qmd to pick up navbar update"
  quarto render home.qmd 2>&1 | Out-String
  $home = Get-Content -Raw _site/home.html
}
if ($home -match 'href="\.?\/?trackers/eu-court\.html"') { "PASS: navbar -> EU Court" } else { "FAIL: navbar -> EU Court" }
```

Expected: PASS.

- [ ] **Step 7: No build artifacts in git**

```powershell
git status --short | Select-String -Pattern "_site|\.quarto|_freeze"
```

Expected: NO output.

- [ ] **Step 8: Stage**

```powershell
git add _quarto.yml AGENTS.md
```

---

## Task 7: Final pre-commit sanity + commit + push

**Files:**
- Commit: 1
- Push: 1

- [ ] **Step 1: Inventory**

```powershell
Set-Location "C:\Users\uctqova\Documents\github\euverse"
git status
```

Expected `Changes to be committed` includes ALL of:
- `modified:   AGENTS.md`
- `modified:   _quarto.yml`
- `modified:   renv.lock`
- `new file:   R/build_eu_member_states.R`
- `new file:   R/fetch_curia_cases.R`
- `new file:   R/fetch_eurlex_court_decisions.R`
- `new file:   data-apis/curia_cases_latest.parquet`
- `new file:   data-apis/curia_cases_latest.meta.json`
- `new file:   data-apis/eurlex_court_decisions_latest.parquet`
- `new file:   data-apis/eurlex_court_decisions_latest.meta.json`
- `new file:   data-final/eu_member_states.parquet`
- `new file:   data-final/eu_member_states.meta.json`
- `new file:   data-manual/eu-member-states.csv`
- `new file:   trackers/eu-court.qmd`

PLUS untracked spec+plan docs (stage next).

- [ ] **Step 2: Stage spec + plan docs**

```powershell
git add docs/specs/2026-05-25-eucourt-eulaw-port-design.md docs/plans/2026-05-25-eucourt-port.md
git status --short
```

Working tree otherwise clean.

- [ ] **Step 3: Largest-files check**

```powershell
git diff --cached --name-only | ForEach-Object { if (Test-Path $_) { $i = Get-Item $_; [pscustomobject]@{ File = $_; KB = [math]::Round($i.Length/1KB, 1) } } } | Sort-Object KB -Descending | Select-Object -First 10
```

Expected: largest is one of `data-apis/eurlex_court_decisions_latest.parquet` (~3–10 MB), `renv.lock` (~310 KB), or the plan doc (~50 KB). Nothing > 50 MB.

- [ ] **Step 4: Write commit message to temp file**

Use the `Write` tool to create `.git_commit_msg.tmp` at the repo root with this EXACT content:

```
Port eu-court tracker + build eu_member_states shared resource

Third tracker port: judicial proceedings in the EU (CJEU caseload,
forecast, decisions by type/formation/rapporteur, preliminary references
map and per-country time series). Also introduces the canonical
data-final/eu_member_states.parquet master table that this port's
eu-court page consumes and the follow-up eu-law port (Plan B) will
also reuse.

Shared-infra additions:
- data-manual/eu-member-states.csv: 28-row hand-curated source of truth
  (EU27 + UK with accession_year and exit_year; founders at 1952).
- R/build_eu_member_states.R: idempotent CSV-to-parquet build producing
  data-final/eu_member_states.parquet + .meta.json. Replaces the
  hardcoded `enlargements`/`accessions`/`ms_years` tables that the old
  eu-court and eu-law Rmds each duplicated.

Fetchers (one per upstream dataset):
- R/fetch_curia_cases.R: wraps eurlex::elx_curia_list() (Curia scraper,
  not SPARQL). Outputs ~60-80K cases since 1953 with court (CJ/GC/CST),
  case_status, year/number, and ECLI parsing. Schema:
  case_id, ecli, court, see_case, case_status, case_year, case_number,
  decision_year, decision_date_str, case_info.
- R/fetch_eurlex_court_decisions.R: SPARQL sector 6 query + ~50-500
  title fetches for missing-procedure inference + procedure_class
  derivation + the small block of static celex-specific corrections
  from the old Rmd. Outputs ~40-60K rows with decision, procedure_class,
  formation_simple, rapporteur, language, origin, ecli.

trackers/eu-court.qmd: 7 sections + DT recent-cases table:
- Caseload bar chart per court (3 Okabe-Ito colours, replaces the old
  bespoke pink/navy/teal trio).
- Forecast model (OLS + Poisson regression on CJ lags + GC lag) with
  modelsummary table. LaTeX equation block and modelplot dropped per
  spec; the modelsummary table remains.
- Decisions: by type x procedure, by formation, top rapporteurs.
- Preliminary references: choropleth (viridis sequential, replaces the
  old pink gradient) + per-country small-multiples time series.
- All charts use theme_mo() and register_ggiraph_defaults();
  per-plot opts_* blocks dropped.
- About-this-page footer with source links + most-recent-case prose.

_quarto.yml: navbar Trackers dropdown now has EU Finance, EU Vetoes,
EU Court.

renv: pins 6 new packages (eurlex, modelsummary, maps, forcats, purrr,
tidyr).

AGENTS.md migration map: 3 of 4 tracker rows now marked ported. Next:
eu-law (the second plan from the combined spec; reuses
eu_member_states; no new R deps needed).

Local smoke render verified all 13 page markers; eu-finance + eu-vetoes
render unchanged.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
```

- [ ] **Step 5: Commit + delete temp file**

```powershell
git commit -F .git_commit_msg.tmp 2>&1 | Out-String
Remove-Item .git_commit_msg.tmp
git log --oneline -3
```

Expected: commit succeeds; `git log --oneline -3` shows the new commit at HEAD.

- [ ] **Step 6: Push**

```powershell
git push origin main 2>&1 | Out-String
```

Expected: push succeeds (`<prev-sha>..<new-sha>  main -> main`). If REJECTED, do NOT `--force`; report BLOCKED.

- [ ] **Step 7: Post-push state**

```powershell
git status
git log --oneline -4
```

Expected: clean working tree, branch up to date, log shows latest commits.

---

## Task 8: Watch CI + verify live site

**Files:** (none — observation only)

- [ ] **Step 1: Poll the workflow run until completion**

Run in a polling loop (~3-5 min expected):

```bash
LATEST_SHA=$(cd "C:/Users/uctqova/Documents/github/euverse" && git rev-parse HEAD | cut -c1-7)
echo "Polling for run on $LATEST_SHA"
for i in $(seq 1 25); do
  STATE=$(curl -s "https://api.github.com/repos/michalovadek/euverse/actions/runs?per_page=1" | python -c "
import json,sys
r = json.load(sys.stdin)['workflow_runs'][0]
print(f\"{r['head_sha'][:7]}|{r['status']}|{r['conclusion']}\")")
  HEAD=$(echo "$STATE" | cut -d'|' -f1)
  STATUS=$(echo "$STATE" | cut -d'|' -f2)
  CONCL=$(echo "$STATE" | cut -d'|' -f3)
  echo "[$(date +%H:%M:%S)] poll $i: head=$HEAD status=$STATUS conclusion=$CONCL"
  if [ "$HEAD" = "$LATEST_SHA" ] && [ "$STATUS" = "completed" ]; then
    echo "FINAL: $STATE"
    break
  fi
  sleep 30
done
```

Expected: ends with `FINAL: <sha>|completed|success`.

- [ ] **Step 2: Verify live site URLs**

```bash
echo "=== home (200 expected) ==="
curl -sI https://michalovadek.github.io/euverse/ | head -1
echo "=== eu-finance (still works) ==="
curl -sI https://michalovadek.github.io/euverse/trackers/eu-finance.html | head -1
echo "=== eu-vetoes (still works) ==="
curl -sI https://michalovadek.github.io/euverse/trackers/eu-vetoes.html | head -1
echo "=== eu-court (NEW) ==="
curl -sI https://michalovadek.github.io/euverse/trackers/eu-court.html | head -1
curl -s https://michalovadek.github.io/euverse/trackers/eu-court.html | grep -oE "Judicial Proceedings|Number of cases|Forecasting|Preliminary references|michalovadek.github.io/euverse/trackers/eu-court.html" | sort -u
```

Expected: 4 lines of `HTTP/2 200`; the grep shows all 5 markers present.

- [ ] **Step 3: Report findings to controller**

Status:
- `render` job: success / failure (with reason)
- `deploy` job: success / failure (with reason)
- Live home: 200 / 404 / other
- Live eu-finance: 200 / broken / other
- Live eu-vetoes: 200 / broken / other
- Live eu-court (NEW): 200 / broken / other
- 5 content markers: PASS / FAIL each

---

## Done criteria

This phase is complete when ALL of:
- One new commit on local `main` AND `origin/main`.
- CI: both `render` and `deploy` succeed for the new commit.
- https://michalovadek.github.io/euverse/trackers/eu-court.html resolves with all 7 sections + forecast model table + choropleth + per-country time series.
- The 3 prior live URLs (home, eu-finance, eu-vetoes) still return 200.
- AGENTS.md migration map shows ✅ for eu-court (3 of 4 tracker rows).
- All 3 tracker pages appear in the Trackers navbar dropdown.
- `_site/`, `.quarto/`, `_freeze/`, `renv/library/`, `.venv/` absent from `git ls-files`.

When all of the above are true, **Plan B (eu-law port)** begins. Plan B will reuse the now-existing `data-final/eu_member_states.parquet` from this commit, add 4 sector-3 SPARQL fetchers, and a similar 6-section page. No additional shared-resource work; no new R packages required.
