# eufinance Port — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Port `old/eufinancestats.Rmd` (the standalone EU finance tracker) into `trackers/eu-finance.qmd` on the euverse repo's new shared infra, with 4 source-named per-dataset fetchers populating `data-apis/`, a single combined freshness badge per page, and the 3 original sections rendered against the new zephyr/darkly theme.

**Architecture:** Four per-upstream-dataset R fetcher scripts (`R/fetch_ecb_yield_curve.R`, `R/fetch_eurostat_teimf050.R`, `R/fetch_ecb_mrr.R`, `R/fetch_ecb_hicp.R`) each write source-named tidy parquets + meta sidecars under `data-apis/`. The new tracker page consumes those parquets via `read_with_freshness(...)`, plots with `theme_mo()` + `ggiraph` + `ggforce::facet_zoom`, and shows one combined freshness badge sourced from a new `freshness_badge_combined()` helper in `R/freshness.R`. Single commit covers renv lock update, helpers, fetchers, initial snapshots, page, and navbar wiring.

**Tech Stack:** R 4.4.3 (with renv 1.2.3); existing renv pin set (arrow, ggplot2, ggiraph, DT, gt, here, jsonlite, viridis, RColorBrewer, digest, htmltools, yaml, dplyr, vroom, haven) **plus** 5 new top-level pins (`eurostat`, `ggforce`, `dplyr`, `lubridate`, `stringr`); Quarto 1.7.17; ECB SDMX API (direct HTTP CSV) + `eurostat` package.

**Spec reference:** `docs/specs/2026-05-24-eufinance-port-design.md`

**Conventions for this plan:**
- Working directory throughout: `C:\Users\uctqova\Documents\github\euverse`. Each command block re-asserts this with `Set-Location` so tasks are self-contained.
- Rscript is not on PATH; use the absolute path `& "C:\Program Files\R\R-4.4.3\bin\Rscript.exe"`.
- All file paths in this plan are absolute or relative to the euverse repo root.
- "Verify" steps run a command and compare output to the "Expected" line. Discrepancy = stop and surface.
- Commits happen only in **Task 11** (one-commit plan per the spec). Earlier tasks `git add` to stage but do not commit.
- For PowerShell here-strings into `Rscript -e`: if the multi-line `@"..."@` pipe to `Rscript -e` produces no visible output (a known quirk on Windows PowerShell 5.1), save the snippet to a temp `.R` file, run `Rscript <file>.R`, then delete the temp file.

---

## Task 1: Install new R deps and snapshot

**Files:**
- Modify: `renv.lock` (regenerated by snapshot)

- [ ] **Step 1: Install the 5 new top-level packages**

Run (PowerShell):
```powershell
Set-Location "C:\Users\uctqova\Documents\github\euverse"
& "C:\Program Files\R\R-4.4.3\bin\Rscript.exe" -e "renv::install(c('eurostat','ggforce','dplyr','lubridate','stringr'), prompt = FALSE)"
```

Expected: installation log shows packages installed (cold this might take ~30–90 s; `ggforce` may compile from source if no binary is cached; `eurostat` pulls `httr`, `xml2`, etc. as transitives). Final line approximately "Successfully installed N packages". No "FAILED" / "non-zero exit status" lines.

- [ ] **Step 2: Snapshot — scope to project library only**

Run (PowerShell):
```powershell
& "C:\Program Files\R\R-4.4.3\bin\Rscript.exe" -e "renv::snapshot(type = 'all', library = renv::paths$library(), prompt = FALSE)"
```

`library = renv::paths$library()` is mandatory — without it, `type = 'all'` snapshots the whole system R library (hundreds of unrelated packages, ~1 MB lockfile, ~30 min CI install). See `docs/plans/2026-05-23-shared-infra-phase.md` Task 2 for the full background.

Expected: output begins "The following package(s) will be updated in the lockfile:" listing the 5 new packages plus their transitive deps. Ends with "Lockfile written to ..." pointing at our `renv.lock`. Resulting `renv.lock` size should grow from ~190 KB to roughly ~220–260 KB.

- [ ] **Step 3: Verify the new packages load**

Run (PowerShell):
```powershell
& "C:\Program Files\R\R-4.4.3\bin\Rscript.exe" -e "for (p in c('eurostat','ggforce','dplyr','lubridate','stringr')) { suppressPackageStartupMessages(library(p, character.only = TRUE)); cat(p, packageVersion(p) |> as.character(), '\n') }"
```

Expected: 5 lines, one per package, each `<pkgname> <version>`. No errors.

- [ ] **Step 4: Stage**

```powershell
git add renv.lock
git status --short
```

Expected: `M  renv.lock` appears in the staged set.

---

## Task 2: Add `freshness_badge_combined()` helper

**Files:**
- Modify: `R/freshness.R` (append a new function)

- [ ] **Step 1: Append the helper to `R/freshness.R`**

Use the `Edit` tool. Add the function at the end of `R/freshness.R` (after the existing `freshness_badge()` definition):

```r

freshness_badge_combined <- function(metas) {
  # Render one badge for a multi-source page based on the OLDEST fetched_at
  # in the input list. Users see worst-case staleness rather than best-case
  # — the honest answer when a page joins data from several sources.
  fetched_times <- vapply(metas, function(m) {
    t <- suppressWarnings(
      as.POSIXct(m$fetched_at, tz = "UTC", format = "%Y-%m-%dT%H:%M:%SZ")
    )
    if (length(t) == 1L && !is.na(t)) as.numeric(t) else NA_real_
  }, numeric(1))
  if (all(is.na(fetched_times))) {
    return(freshness_badge(list(
      source = "(multiple)", fetched_at = NA, rows = NA
    )))
  }
  oldest_idx <- which.min(fetched_times)
  oldest_meta <- metas[[oldest_idx]]
  oldest_meta$source <- paste0(
    oldest_meta$source, " (oldest of ", length(metas), " sources)"
  )
  freshness_badge(oldest_meta)
}
```

- [ ] **Step 2: Verify the helper works end-to-end**

Save the following to a temp file `tmp_check_combined.R`, run it, then delete the temp file:

```r
suppressPackageStartupMessages(library(htmltools))
source("R/freshness.R")
now_iso <- format(Sys.time(), "%Y-%m-%dT%H:%M:%SZ", tz = "UTC")
old_iso <- format(Sys.time() - 5 * 86400, "%Y-%m-%dT%H:%M:%SZ", tz = "UTC")

# Case A: three metas with one clearly oldest → should pick old_iso, important
metas <- list(
  list(source = "a", fetched_at = now_iso, rows = 1L),
  list(source = "b", fetched_at = old_iso, rows = 1L),
  list(source = "c", fetched_at = now_iso, rows = 1L)
)
out <- as.character(freshness_badge_combined(metas))
stopifnot(
  grepl("callout-important", out),
  grepl("oldest of 3 sources", out),
  grepl("b \\(oldest", out)
)

# Case B: all metas malformed → returns a graceful "unknown" badge
metas_bad <- list(
  list(source = NA, fetched_at = NA, rows = NA),
  list(source = NA, fetched_at = "garbage", rows = NA)
)
out_bad <- as.character(freshness_badge_combined(metas_bad))
stopifnot(grepl("unknown", out_bad))
cat("freshness_badge_combined OK\n")
```

Run (PowerShell):
```powershell
& "C:\Program Files\R\R-4.4.3\bin\Rscript.exe" tmp_check_combined.R
Remove-Item tmp_check_combined.R
```

Expected: `freshness_badge_combined OK`.

- [ ] **Step 3: Stage**

```powershell
git add R/freshness.R
```

---

## Task 3: `R/fetch_ecb_yield_curve.R`

**Files:**
- Create: `R/fetch_ecb_yield_curve.R`
- Produce: `data-apis/ecb_yield_curve_latest.parquet` + `_latest.meta.json` (committed in Task 11)

- [ ] **Step 1: Write the fetcher**

Create `R/fetch_ecb_yield_curve.R` with this exact content:

```r
# fetch_ecb_yield_curve.R
# Euro Area government bond yield curve spot rates from the ECB SDMX API,
# 1-year, 5-year, 10-year maturities, AAA issuers only. Daily frequency.
# Written as a single tidy long-form parquet.

suppressPackageStartupMessages({
  library(arrow)
  library(jsonlite)
  library(here)
  library(digest)
})

src     <- "ecb_yield_curve"
out_dir <- here::here("data-apis")

# ---- 1. fetch ---------------------------------------------------------------
df <- tryCatch({
  ecb_api <- "https://data-api.ecb.europa.eu/service/data/"
  date_today <- format(Sys.Date(), "%Y-%m-%d")
  maturities <- c(`1` = 1L, `5` = 5L, `10` = 10L)

  parts <- lapply(names(maturities), function(y_chr) {
    url <- paste0(
      ecb_api,
      "YC/B.U2.EUR.4F.G_N_A.SV_C_YM.SR_", y_chr, "Y",
      "?startPeriod=2004-09-06",
      "&endPeriod=", date_today,
      "&format=csvdata"
    )
    raw <- read.csv(url)
    data.frame(
      date           = as.Date(raw$TIME_PERIOD),
      maturity_years = maturities[[y_chr]],
      yield_pct      = as.numeric(raw$OBS_VALUE)
    )
  })
  do.call(rbind, parts)
}, error = function(e) {
  message("Fetch failed: ", conditionMessage(e))
  quit(status = 1L)
})

# ---- 2. validate ------------------------------------------------------------
if (!is.data.frame(df) || nrow(df) == 0L) {
  message("Validation failed: empty or non-data.frame result")
  quit(status = 1L)
}
required_cols <- c("date", "maturity_years", "yield_pct")
if (!all(required_cols %in% names(df))) {
  message("Validation failed: missing columns ",
          paste(setdiff(required_cols, names(df)), collapse = ", "))
  quit(status = 1L)
}
if (!all(df$maturity_years %in% c(1L, 5L, 10L))) {
  message("Validation failed: unexpected maturity values")
  quit(status = 1L)
}

# ---- 3. write atomically ----------------------------------------------------
data_tmp <- tempfile(tmpdir = out_dir, fileext = ".parquet")
meta_tmp <- tempfile(tmpdir = out_dir, fileext = ".json")

arrow::write_parquet(df, data_tmp, compression = "zstd")
jsonlite::write_json(
  list(
    source      = src,
    fetched_at  = format(Sys.time(), "%Y-%m-%dT%H:%M:%SZ", tz = "UTC"),
    rows        = nrow(df),
    schema_hash = digest::digest(names(df), algo = "sha256")
  ),
  meta_tmp, auto_unbox = TRUE, pretty = TRUE
)

file.rename(data_tmp, file.path(out_dir, paste0(src, "_latest.parquet")))
file.rename(meta_tmp, file.path(out_dir, paste0(src, "_latest.meta.json")))
message("Wrote ", src, " latest snapshot: ", nrow(df), " rows.")
```

- [ ] **Step 2: Run the fetcher**

```powershell
Set-Location "C:\Users\uctqova\Documents\github\euverse"
& "C:\Program Files\R\R-4.4.3\bin\Rscript.exe" R/fetch_ecb_yield_curve.R
```

Expected: final line approximately `Wrote ecb_yield_curve latest snapshot: <N> rows.` where N is in the tens of thousands (daily data × 3 maturities × ~20 years). Network call to `data-api.ecb.europa.eu` takes a few seconds.

- [ ] **Step 3: Verify the snapshot shape**

```powershell
& "C:\Program Files\R\R-4.4.3\bin\Rscript.exe" -e "df <- arrow::read_parquet('data-apis/ecb_yield_curve_latest.parquet'); cat('rows:', nrow(df), 'cols:', paste(names(df), collapse=','), '\nmaturities:', paste(sort(unique(df$maturity_years)), collapse=','), '\ndate range:', as.character(min(df$date)), 'to', as.character(max(df$date)), '\n'); m <- jsonlite::fromJSON('data-apis/ecb_yield_curve_latest.meta.json'); cat('meta source:', m$source, 'rows:', m$rows, '\n')"
```

Expected: rows in the tens of thousands; cols = `date,maturity_years,yield_pct`; maturities = `1,5,10`; date range starts 2004-09-06 and ends within the last 2 weeks; meta source = `ecb_yield_curve` and meta rows matches.

- [ ] **Step 4: Stage**

```powershell
git add R/fetch_ecb_yield_curve.R data-apis/ecb_yield_curve_latest.parquet data-apis/ecb_yield_curve_latest.meta.json
```

---

## Task 4: `R/fetch_eurostat_teimf050.R`

**Files:**
- Create: `R/fetch_eurostat_teimf050.R`
- Produce: `data-apis/eurostat_teimf050_latest.parquet` + `_latest.meta.json`

- [ ] **Step 1: Write the fetcher**

Create `R/fetch_eurostat_teimf050.R` with this exact content:

```r
# fetch_eurostat_teimf050.R
# Eurostat "teimf050": long-term gov bond yields, 10-year maturity, monthly,
# by country (incl. EA + EU27_2020 aggregates which the page filters as needed).
# Written as a single tidy parquet with English country names joined.

suppressPackageStartupMessages({
  library(arrow); library(jsonlite); library(here); library(digest)
  library(eurostat); library(dplyr); library(tibble)
})

src     <- "eurostat_teimf050"
out_dir <- here::here("data-apis")

# ---- 1. fetch ---------------------------------------------------------------
df <- tryCatch({
  raw <- eurostat::get_eurostat("teimf050")
  # Build a code -> name lookup. eurostat::eu_countries is the EU27 list with
  # codes; we add UK + the EA / EU27_2020 aggregates so country_name is never NA.
  country_map <- dplyr::bind_rows(
    eurostat::eu_countries,
    tibble::tibble(
      code = c("UK", "EA", "EU27_2020"),
      name = c("United Kingdom", "Euro Area", "EU27 (2020)")
    )
  )
  raw |>
    dplyr::left_join(country_map, by = c("geo" = "code")) |>
    dplyr::transmute(
      date         = as.Date(TIME_PERIOD),
      geo          = as.character(geo),
      country_name = as.character(name),
      yield_pct    = as.numeric(values)
    ) |>
    as.data.frame()
}, error = function(e) {
  message("Fetch failed: ", conditionMessage(e))
  quit(status = 1L)
})

# ---- 2. validate ------------------------------------------------------------
if (!is.data.frame(df) || nrow(df) == 0L) {
  message("Validation failed: empty or non-data.frame result")
  quit(status = 1L)
}
required_cols <- c("date", "geo", "country_name", "yield_pct")
if (!all(required_cols %in% names(df))) {
  message("Validation failed: missing columns ",
          paste(setdiff(required_cols, names(df)), collapse = ", "))
  quit(status = 1L)
}
if (!"EA" %in% df$geo) {
  message("Validation failed: EA aggregate missing from snapshot")
  quit(status = 1L)
}

# ---- 3. write atomically ----------------------------------------------------
data_tmp <- tempfile(tmpdir = out_dir, fileext = ".parquet")
meta_tmp <- tempfile(tmpdir = out_dir, fileext = ".json")
arrow::write_parquet(df, data_tmp, compression = "zstd")
jsonlite::write_json(
  list(
    source      = src,
    fetched_at  = format(Sys.time(), "%Y-%m-%dT%H:%M:%SZ", tz = "UTC"),
    rows        = nrow(df),
    schema_hash = digest::digest(names(df), algo = "sha256")
  ),
  meta_tmp, auto_unbox = TRUE, pretty = TRUE
)
file.rename(data_tmp, file.path(out_dir, paste0(src, "_latest.parquet")))
file.rename(meta_tmp, file.path(out_dir, paste0(src, "_latest.meta.json")))
message("Wrote ", src, " latest snapshot: ", nrow(df), " rows.")
```

- [ ] **Step 2: Run the fetcher**

```powershell
& "C:\Program Files\R\R-4.4.3\bin\Rscript.exe" R/fetch_eurostat_teimf050.R
```

Expected: final line `Wrote eurostat_teimf050 latest snapshot: <N> rows.` where N is several thousand (monthly × ~30 countries × ~25 years). `eurostat::get_eurostat` may print a "Reading cache file" or "Downloading" message — both are fine.

**Possible quirk**: if `eurostat::get_eurostat` returns lowercase `time` instead of `TIME_PERIOD`, the `transmute(date = as.Date(TIME_PERIOD), …)` line errors with "object not found". If that happens, change `TIME_PERIOD` and `values` in the transmute to whatever column names the package version actually returns (inspect with `names(raw)` after the `get_eurostat` call).

- [ ] **Step 3: Verify the snapshot shape**

```powershell
& "C:\Program Files\R\R-4.4.3\bin\Rscript.exe" -e "df <- arrow::read_parquet('data-apis/eurostat_teimf050_latest.parquet'); cat('rows:', nrow(df), 'cols:', paste(names(df), collapse=','), '\ngeo distinct:', length(unique(df$geo)), 'countries\n'); ms <- df[df$geo == 'EA',]; cat('EA observations:', nrow(ms), '\n'); m <- jsonlite::fromJSON('data-apis/eurostat_teimf050_latest.meta.json'); cat('meta source:', m$source, 'rows:', m$rows, '\n')"
```

Expected: rows in the thousands; cols = `date,geo,country_name,yield_pct`; geo distinct ≥ 28 (EU27 + UK + EA + EU27_2020 = 30, possibly more); EA observations ≥ 100 monthly; meta source = `eurostat_teimf050`.

- [ ] **Step 4: Stage**

```powershell
git add R/fetch_eurostat_teimf050.R data-apis/eurostat_teimf050_latest.parquet data-apis/eurostat_teimf050_latest.meta.json
```

---

## Task 5: `R/fetch_ecb_mrr.R`

**Files:**
- Create: `R/fetch_ecb_mrr.R`
- Produce: `data-apis/ecb_mrr_latest.parquet` + `_latest.meta.json`

- [ ] **Step 1: Write the fetcher**

Create `R/fetch_ecb_mrr.R` with this exact content:

```r
# fetch_ecb_mrr.R
# ECB key interest rate on Main Refinancing Operations (MRR_FR.LEV).
# Event-driven series — one observation per rate change. Goes back to 1998.

suppressPackageStartupMessages({
  library(arrow); library(jsonlite); library(here); library(digest)
})

src     <- "ecb_mrr"
out_dir <- here::here("data-apis")

# ---- 1. fetch ---------------------------------------------------------------
df <- tryCatch({
  ecb_api <- "https://data-api.ecb.europa.eu/service/data/"
  date_today <- format(Sys.Date(), "%Y-%m-%d")
  url <- paste0(
    ecb_api,
    "FM/B.U2.EUR.4F.KR.MRR_FR.LEV",
    "?startPeriod=1998-01-01",
    "&endPeriod=", date_today,
    "&format=csvdata"
  )
  raw <- read.csv(url)
  data.frame(
    date     = as.Date(raw$TIME_PERIOD),
    rate_pct = as.numeric(raw$OBS_VALUE)
  )
}, error = function(e) {
  message("Fetch failed: ", conditionMessage(e))
  quit(status = 1L)
})

# ---- 2. validate ------------------------------------------------------------
if (!is.data.frame(df) || nrow(df) == 0L) {
  message("Validation failed: empty or non-data.frame result")
  quit(status = 1L)
}
if (!all(c("date", "rate_pct") %in% names(df))) {
  message("Validation failed: missing columns")
  quit(status = 1L)
}

# ---- 3. write atomically ----------------------------------------------------
data_tmp <- tempfile(tmpdir = out_dir, fileext = ".parquet")
meta_tmp <- tempfile(tmpdir = out_dir, fileext = ".json")
arrow::write_parquet(df, data_tmp, compression = "zstd")
jsonlite::write_json(
  list(
    source      = src,
    fetched_at  = format(Sys.time(), "%Y-%m-%dT%H:%M:%SZ", tz = "UTC"),
    rows        = nrow(df),
    schema_hash = digest::digest(names(df), algo = "sha256")
  ),
  meta_tmp, auto_unbox = TRUE, pretty = TRUE
)
file.rename(data_tmp, file.path(out_dir, paste0(src, "_latest.parquet")))
file.rename(meta_tmp, file.path(out_dir, paste0(src, "_latest.meta.json")))
message("Wrote ", src, " latest snapshot: ", nrow(df), " rows.")
```

- [ ] **Step 2: Run the fetcher**

```powershell
& "C:\Program Files\R\R-4.4.3\bin\Rscript.exe" R/fetch_ecb_mrr.R
```

Expected: final line `Wrote ecb_mrr latest snapshot: <N> rows.` where N is approximately 50–80 (event-driven since 1998).

- [ ] **Step 3: Verify the snapshot shape**

```powershell
& "C:\Program Files\R\R-4.4.3\bin\Rscript.exe" -e "df <- arrow::read_parquet('data-apis/ecb_mrr_latest.parquet'); cat('rows:', nrow(df), 'cols:', paste(names(df), collapse=','), '\nrate range pct:', min(df$rate_pct), 'to', max(df$rate_pct), '\ndate range:', as.character(min(df$date)), 'to', as.character(max(df$date)), '\n')"
```

Expected: rows 40–100; cols = `date,rate_pct`; rate range somewhere between 0 and ~5%; date range starts in 1998 or 1999, ends within last few months (only updates when ECB changes the rate).

- [ ] **Step 4: Stage**

```powershell
git add R/fetch_ecb_mrr.R data-apis/ecb_mrr_latest.parquet data-apis/ecb_mrr_latest.meta.json
```

---

## Task 6: `R/fetch_ecb_hicp.R`

**Files:**
- Create: `R/fetch_ecb_hicp.R`
- Produce: `data-apis/ecb_hicp_latest.parquet` + `_latest.meta.json`

- [ ] **Step 1: Write the fetcher**

Create `R/fetch_ecb_hicp.R` with this exact content:

```r
# fetch_ecb_hicp.R
# ECB Harmonised Index of Consumer Prices, Euro Area, year-on-year annual
# rate of change. Monthly. Goes back to 1998.

suppressPackageStartupMessages({
  library(arrow); library(jsonlite); library(here); library(digest)
  library(lubridate)
})

src     <- "ecb_hicp"
out_dir <- here::here("data-apis")

# ---- 1. fetch ---------------------------------------------------------------
df <- tryCatch({
  ecb_api <- "https://data-api.ecb.europa.eu/service/data/"
  date_today <- format(Sys.Date(), "%Y-%m-%d")
  url <- paste0(
    ecb_api,
    "ICP/M.U2.N.000000.4.ANR",
    "?startPeriod=1998-01-01",
    "&endPeriod=", date_today,
    "&format=csvdata"
  )
  raw <- read.csv(url)
  # TIME_PERIOD is "YYYY-MM"; convert to a Date at first of month.
  data.frame(
    date    = as.Date(lubridate::ym(raw$TIME_PERIOD)),
    yoy_pct = as.numeric(raw$OBS_VALUE)
  )
}, error = function(e) {
  message("Fetch failed: ", conditionMessage(e))
  quit(status = 1L)
})

# ---- 2. validate ------------------------------------------------------------
if (!is.data.frame(df) || nrow(df) == 0L) {
  message("Validation failed: empty or non-data.frame result")
  quit(status = 1L)
}
if (!all(c("date", "yoy_pct") %in% names(df))) {
  message("Validation failed: missing columns")
  quit(status = 1L)
}

# ---- 3. write atomically ----------------------------------------------------
data_tmp <- tempfile(tmpdir = out_dir, fileext = ".parquet")
meta_tmp <- tempfile(tmpdir = out_dir, fileext = ".json")
arrow::write_parquet(df, data_tmp, compression = "zstd")
jsonlite::write_json(
  list(
    source      = src,
    fetched_at  = format(Sys.time(), "%Y-%m-%dT%H:%M:%SZ", tz = "UTC"),
    rows        = nrow(df),
    schema_hash = digest::digest(names(df), algo = "sha256")
  ),
  meta_tmp, auto_unbox = TRUE, pretty = TRUE
)
file.rename(data_tmp, file.path(out_dir, paste0(src, "_latest.parquet")))
file.rename(meta_tmp, file.path(out_dir, paste0(src, "_latest.meta.json")))
message("Wrote ", src, " latest snapshot: ", nrow(df), " rows.")
```

- [ ] **Step 2: Run the fetcher**

```powershell
& "C:\Program Files\R\R-4.4.3\bin\Rscript.exe" R/fetch_ecb_hicp.R
```

Expected: final line `Wrote ecb_hicp latest snapshot: <N> rows.` where N is approximately 300–400 (monthly since 1998).

- [ ] **Step 3: Verify the snapshot shape**

```powershell
& "C:\Program Files\R\R-4.4.3\bin\Rscript.exe" -e "df <- arrow::read_parquet('data-apis/ecb_hicp_latest.parquet'); cat('rows:', nrow(df), 'cols:', paste(names(df), collapse=','), '\nyoy range pct:', round(min(df$yoy_pct, na.rm=TRUE), 2), 'to', round(max(df$yoy_pct, na.rm=TRUE), 2), '\ndate range:', as.character(min(df$date)), 'to', as.character(max(df$date)), '\n')"
```

Expected: rows ~330; cols = `date,yoy_pct`; yoy range historical ~ -0.5 to ~10 pct; date range 1998-01-01 to within the last 2 months (HICP data lags by ~3 weeks).

- [ ] **Step 4: Stage**

```powershell
git add R/fetch_ecb_hicp.R data-apis/ecb_hicp_latest.parquet data-apis/ecb_hicp_latest.meta.json
```

---

## Task 7: Update `_quarto.yml` navbar

**Files:**
- Modify: `_quarto.yml` (replace the `navbar:` block)

- [ ] **Step 1: Replace the navbar block**

Use the `Edit` tool on `_quarto.yml`. Replace:

```yaml
  navbar:
    left:
      - href: home.qmd
        text: Home
```

With:

```yaml
  navbar:
    left:
      - href: home.qmd
        text: Home
      - text: Trackers
        menu:
          - href: trackers/eu-finance.qmd
            text: EU Finance
```

- [ ] **Step 2: Verify the YAML still parses**

```powershell
Set-Location "C:\Users\uctqova\Documents\github\euverse"
uv run --with pyyaml python -c "import yaml; d=yaml.safe_load(open('_quarto.yml')); print('navbar.left:', d['website']['navbar']['left'])"
```

Expected: prints a list with two entries: the Home href entry and the Trackers menu entry (whose `menu` contains the EU Finance entry).

- [ ] **Step 3: Stage**

```powershell
git add _quarto.yml
```

---

## Task 8: Create `trackers/eu-finance.qmd`

**Files:**
- Create: `trackers/eu-finance.qmd`

- [ ] **Step 1: Create the trackers directory**

```powershell
Set-Location "C:\Users\uctqova\Documents\github\euverse"
New-Item -ItemType Directory -Force -Path trackers | Out-Null
Test-Path trackers
```

Expected: `True`.

- [ ] **Step 2: Write the page**

Create `trackers/eu-finance.qmd` with this exact content (note the file uses 3-backtick R chunks; the outer 4-backtick wrapping in this prompt is just for display):

````markdown
---
title: "Borrowing and Yields in the European Union"
subtitle: "An automatically updated overview of the EU's finances"
toc: true
---

```{r setup, include=FALSE}
suppressPackageStartupMessages({
  library(ggplot2)
  library(ggiraph)
  library(ggforce)
  library(dplyr)
  library(lubridate)
  library(stringr)
  library(here)
})
source(here::here("R", "theme.R"))
source(here::here("R", "freshness.R"))
register_ggiraph_defaults()

# Load all four snapshots once at the top of the page.
yc  <- read_with_freshness("ecb_yield_curve")
eus <- read_with_freshness("eurostat_teimf050")
mrr <- read_with_freshness("ecb_mrr")
hic <- read_with_freshness("ecb_hicp")

today_d <- Sys.Date()
```

The European Union is not only a single market but also an economic and
monetary union. Most members share the euro, which entails a supranational
monetary policy run by the European Central Bank (ECB) together with Euro
Area central banks. This page provides an automatically updated overview of
several indicators central to that policy, sourced from the
[ECB Data Portal API](https://data.ecb.europa.eu/help/api/) and Eurostat.

```{r freshness, results='asis', echo=FALSE}
freshness_badge_combined(list(yc$meta, eus$meta, mrr$meta, hic$meta))
```

# Euro Area government bond yields

Government borrowing is normally carried out through bond sales. Bonds entitle
the buyer to an interest rate (coupon) and the return of the principal at
maturity. Bonds trade on secondary markets and their yields fluctuate
accordingly.

```{r euro-area-yields, echo=FALSE, fig.height=4.5, fig.width=8.5}
df_yc <- yc$data |>
  mutate(
    maturity_label = paste0(maturity_years, "-year maturity"),
    maturity_label = factor(maturity_label,
                            levels = c("1-year maturity",
                                       "5-year maturity",
                                       "10-year maturity"))
  )

p_yc <- ggplot(df_yc, aes(x = date, y = yield_pct,
                          colour = maturity_label,
                          tooltip = maturity_label,
                          data_id = maturity_label)) +
  geom_hline(yintercept = 0, colour = "grey80") +
  geom_line_interactive(linewidth = 0.3, show.legend = FALSE) +
  scale_y_continuous(labels = function(x) paste0(x, "%")) +
  scale_colour_manual(values = mo_palette$categorical[1:3]) +
  ggforce::facet_zoom(xy = between(date, today_d - 365, today_d),
                      horizontal = FALSE, zoom.size = 0.5) +
  labs(x = NULL, y = "Yield",
       title = "Euro Area government bond yields",
       subtitle = "Daily spot rates, triple-A issuers only") +
  theme_mo()

girafe(ggobj = p_yc, width_svg = 8.5, height_svg = 4.5)
```

Lending money for longer is normally associated with higher yields; when
short-term yields overtake long-term yields, the yield curve has inverted,
which has historically been predictive of recessions.

# Government bond yields by country

```{r country-yields-prep, include=FALSE}
country_data <- eus$data |>
  filter(!geo %in% c("EA", "EU27_2020"))

# Latest observation per country, then rank for max/min prose.
cur_latest <- country_data |>
  arrange(desc(date)) |>
  group_by(country_name) |>
  slice(1) |>
  ungroup()

cur_max_yld <- cur_latest |> slice_max(yield_pct, n = 1)
cur_min_yld <- cur_latest |> slice_min(yield_pct, n = 1)

# Euro Area average (last 12 months) and non-euro EU-member average.
ea_recent <- eus$data |>
  filter(geo == "EA", date >= today_d - 365) |>
  summarise(mean = round(mean(yield_pct, na.rm = TRUE), 2))

non_euro_geos <- c("CZ", "PL", "HU", "RO", "BG", "SE", "DK")
non_ea_recent <- country_data |>
  filter(geo %in% non_euro_geos, date >= today_d - 365) |>
  summarise(mean = round(mean(yield_pct, na.rm = TRUE), 2))
```

```{r country-yields-plot, echo=FALSE, fig.height=7, fig.width=9}
ea_series <- eus$data |> filter(geo == "EA") |> select(-geo, -country_name)

p_country <- ggplot(country_data,
                    aes(x = date, y = yield_pct)) +
  geom_line(data = ea_series, aes(x = date, y = yield_pct),
            colour = mo_palette$categorical[5], linetype = 2) +
  geom_line_interactive(colour = "grey55", linewidth = 0.3) +
  geom_point_interactive(
    aes(tooltip = paste0(country_name, ": ", round(yield_pct, 2), "% (",
                         format(date, "%Y-%m"), ")"),
        data_id  = paste0(geo, format(date, "-%Y-%m"))),
    size = 0.45, colour = "grey35"
  ) +
  scale_y_continuous(labels = function(x) paste0(x, "%")) +
  facet_wrap(~ country_name) +
  labs(x = NULL, y = "Yield",
       title = "Government bond yields in the EU and UK",
       subtitle = "10-year maturity, monthly data",
       caption = "Dashed line: Euro Area average") +
  theme_mo() +
  theme(axis.text.x = element_text(size = 6),
        strip.text  = element_text(hjust = 1))

girafe(ggobj = p_country, width_svg = 9, height_svg = 7)
```

As most EU borrowing happens at the national level, yields vary
systematically by country as well as over time. At the moment the highest
yields are on bonds issued by **`r cur_max_yld$country_name`** at
**`r round(cur_max_yld$yield_pct, 2)`%**; the lowest are on
**`r cur_min_yld$country_name`** at **`r round(cur_min_yld$yield_pct, 2)`%**.
Over the last twelve months, the Euro Area aggregate yield averaged
**`r ea_recent$mean`%**, while EU members outside the euro averaged
**`r non_ea_recent$mean`%**. The simplistic comparison is confounded by
uneven group composition (macroeconomic stability, debt loads), but
comparing matched pairs across the divide — e.g. Czechia vs. Slovakia, or
Finland vs. Sweden — can be instructive.

# Euro Area key interest rate and inflation

The ECB's primary mandate is price stability, with a long-term inflation
target of 2% year-on-year.

```{r rates-and-inflation, echo=FALSE, fig.height=4.5, fig.width=8.5}
# Join the two series on a daily-frequency axis (rates are event-driven,
# inflation is monthly; carry both forward to make the step plot work).
days <- data.frame(date = seq.Date(as.Date("1998-01-01"), today_d, by = "day"))

mrr_daily <- days |>
  left_join(mrr$data, by = "date") |>
  arrange(date) |>
  mutate(value = vctrs::vec_fill_missing(rate_pct, direction = "down")) |>
  transmute(date, series = "ECB main refinancing rate", value)

hic_daily <- days |>
  left_join(hic$data, by = "date") |>
  arrange(date) |>
  mutate(value = vctrs::vec_fill_missing(yoy_pct, direction = "down")) |>
  transmute(date, series = "HICP year-on-year inflation", value)

rates_long <- bind_rows(mrr_daily, hic_daily) |> filter(!is.na(value))

p_rates <- ggplot(rates_long,
                  aes(x = date, y = value,
                      colour = series,
                      tooltip = series,
                      data_id = series)) +
  geom_hline(yintercept = 0, colour = "grey80") +
  geom_hline(yintercept = 2, colour = "grey50", linetype = 2) +
  geom_step_interactive(linewidth = 0.35, show.legend = FALSE) +
  annotate("text", x = as.Date("1998-11-01"), y = 2.4,
           label = "2% inflation target", size = 2.6, colour = "grey40") +
  scale_y_continuous(labels = function(x) paste0(x, "%")) +
  scale_colour_manual(values = mo_palette$categorical[c(2, 6)]) +
  ggforce::facet_zoom(x = between(date, today_d - 365, today_d),
                      horizontal = FALSE, zoom.size = 0.5) +
  labs(x = NULL, y = "Rate",
       title = "Key interest rate and inflation",
       subtitle = "ECB main refinancing operations rate and Euro Area HICP year-on-year change") +
  theme_mo()

girafe(ggobj = p_rates, width_svg = 8.5, height_svg = 4.5)
```

In times of rising inflation, the ECB's main countervailing instrument is
to raise key interest rates.

# Sources

- ECB Data Portal API — [yield curve spot rates](https://data.ecb.europa.eu/data/datasets/YC), [main refinancing rate](https://data.ecb.europa.eu/data/datasets/FM), [HICP year-on-year inflation](https://data.ecb.europa.eu/data/datasets/ICP).
- Eurostat — long-term gov bond yields, monthly, by country: dataset code `teimf050`.

# Cite

Ovádek, M. *Borrowing and Yields in the European Union*. Accessed `r format(today_d, "%d %B %Y")`. Available at <https://michalovadek.github.io/euverse/trackers/eu-finance.html>.
````

- [ ] **Step 3: Verify the qmd structure (chunk count, no syntax errors at parse)**

```powershell
$qmd = Get-Content -Raw trackers/eu-finance.qmd
$chunks = ([regex]'(?m)^```\{r ').Matches($qmd).Count
"r chunks: $chunks"
"file size: $((Get-Item trackers/eu-finance.qmd).Length) bytes"
```

Expected: `r chunks: 5` (setup, freshness, euro-area-yields, country-yields-prep, country-yields-plot, rates-and-inflation — actually 6, the count depends on chunk-header detection; ≥5 is correct). File size 5–8 KB.

- [ ] **Step 4: Stage**

```powershell
git add trackers/eu-finance.qmd
```

---

## Task 9: Local smoke render + verification

**Files:**
- Generated: `_site/trackers/eu-finance.html` (gitignored)
- Generated: `_site/home.html`, `_site/index.html` (redirect), etc.

- [ ] **Step 1: Render the site**

```powershell
Set-Location "C:\Users\uctqova\Documents\github\euverse"
quarto render 2>&1 | Select-String -Pattern "Output created|Error|ERROR|WARN" | Out-String
```

Expected: at minimum `Output created: _site\trackers\eu-finance.html` AND `Output created: _site\home.html`. Any `Error`/`ERROR` line is a stop-the-line: surface it and do NOT proceed. `WARN` is informational. The Windows temp-cleanup `os error 32` line at the very end is benign — ignore.

- [ ] **Step 2: Verify the tracker page has the expected markers**

```powershell
$html = Get-Content -Raw _site/trackers/eu-finance.html
$checks = @(
  @{ name = "page title";                pattern = "Borrowing and Yields in the European Union" },
  @{ name = "freshness badge";           pattern = "Data as of" },
  @{ name = "combined-badge marker";     pattern = "oldest of 4 sources" },
  @{ name = "Section A heading";         pattern = "Euro Area government bond yields" },
  @{ name = "Section B heading";         pattern = "Government bond yields by country" },
  @{ name = "Section C heading";         pattern = "Euro Area key interest rate and inflation" },
  @{ name = "ggiraph SVG present";       pattern = "<svg" },
  @{ name = "Okabe-Ito hex #E69F00";     pattern = "#E69F00" },
  @{ name = "Okabe-Ito hex #56B4E9";     pattern = "#56B4E9" },
  @{ name = "Okabe-Ito hex #009E73";     pattern = "#009E73" },
  @{ name = "country-yields prose";      pattern = "highest yields are on bonds" },
  @{ name = "citation link to site URL"; pattern = "michalovadek.github.io/euverse/trackers/eu-finance.html" }
)
foreach ($c in $checks) {
  if ($html -match $c.pattern) { "PASS: $($c.name)" } else { "FAIL: $($c.name)" }
}
```

Expected: 12 PASS lines, 0 FAIL. If any FAIL, stop and report — likely root cause is either a fetcher producing a different column name than expected, or a chunk failing silently. Look at the full `quarto render` output for hints.

- [ ] **Step 3: Verify navbar landed in home.html and points to the tracker**

```powershell
$home = Get-Content -Raw _site/home.html
if ($home -match 'href="trackers/eu-finance.html"') { "PASS: navbar links to tracker" } else { "FAIL: navbar link missing" }
if ($home -match 'Trackers') { "PASS: Trackers dropdown label" } else { "FAIL: Trackers dropdown label" }
```

Expected: both PASS.

- [ ] **Step 4: Verify nothing got accidentally gitignored or clobbered**

```powershell
git status --short | Select-String -Pattern "_site|\.quarto|_freeze"
```

Expected: no output (those paths are gitignored).

---

## Task 10: Update AGENTS.md migration map

**Files:**
- Modify: `AGENTS.md` (mark eu-finance row done)

- [ ] **Step 1: Edit the migration-map table**

Use the `Edit` tool on `AGENTS.md`. Find:

```
| `old/eufinancestats.Rmd`                           | Rmd + nightly cron | `trackers/eu-finance.qmd` | `data-apis/ecb_*, eurostat_*`|
```

Replace with:

```
| `old/eufinancestats.Rmd` ✅ ported 2026-05-24      | Rmd + nightly cron | `trackers/eu-finance.qmd` | `data-apis/ecb_*, eurostat_*`|
```

- [ ] **Step 2: Stage**

```powershell
git add AGENTS.md
```

---

## Task 11: Final sanity check + commit + push

**Files:**
- Commit: 1
- Push: 1

- [ ] **Step 1: Inventory of what will be in the commit**

```powershell
Set-Location "C:\Users\uctqova\Documents\github\euverse"
git status
```

Expected: staged set (`Changes to be committed`) includes AT MINIMUM:
- `modified:   AGENTS.md`
- `modified:   _quarto.yml`
- `new file:   R/fetch_ecb_hicp.R`
- `new file:   R/fetch_ecb_mrr.R`
- `new file:   R/fetch_ecb_yield_curve.R`
- `new file:   R/fetch_eurostat_teimf050.R`
- `modified:   R/freshness.R`
- `modified:   renv.lock`
- `new file:   data-apis/ecb_hicp_latest.meta.json`
- `new file:   data-apis/ecb_hicp_latest.parquet`
- `new file:   data-apis/ecb_mrr_latest.meta.json`
- `new file:   data-apis/ecb_mrr_latest.parquet`
- `new file:   data-apis/ecb_yield_curve_latest.meta.json`
- `new file:   data-apis/ecb_yield_curve_latest.parquet`
- `new file:   data-apis/eurostat_teimf050_latest.meta.json`
- `new file:   data-apis/eurostat_teimf050_latest.parquet`
- `new file:   docs/plans/2026-05-24-eufinance-port.md`
- `new file:   docs/specs/2026-05-24-eufinance-port-design.md`
- `new file:   trackers/eu-finance.qmd`

Working tree should be otherwise clean. No `_site/`, `.quarto/`, `_freeze/`, `renv/library/`, `.venv/` anywhere.

- [ ] **Step 2: Stage the spec + plan docs (not staged earlier)**

```powershell
git add docs/specs/2026-05-24-eufinance-port-design.md docs/plans/2026-05-24-eufinance-port.md
git status --short
```

Expected: both files now appear with `A`.

- [ ] **Step 3: Largest-files check**

```powershell
git diff --cached --name-only | ForEach-Object { if (Test-Path $_) { $i = Get-Item $_; [pscustomobject]@{ File = $_; KB = [math]::Round($i.Length/1KB, 1) } } } | Sort-Object KB -Descending | Select-Object -First 10
```

Expected: largest staged file is one of `data-apis/eurostat_teimf050_latest.parquet` (~50–200 KB) or `renv.lock` (~220–260 KB). Nothing > 5000 KB.

- [ ] **Step 4: Write the commit message to a temp file (avoid PowerShell heredoc quirks)**

Use the `Write` tool to create `.git_commit_msg.tmp` at the repo root with this exact content:

```
Port eufinance tracker to euverse shared infra

First tracker port on the new euverse Quarto site. Reproduces the
3-section content of old/eufinancestats.Rmd against the shared theme
(zephyr+darkly, Inter, Okabe-Ito) using per-upstream-dataset fetchers
that other future trackers can reuse without re-querying.

Adds:
- R/fetch_ecb_yield_curve.R  -> data-apis/ecb_yield_curve_latest.{parquet,meta.json}
                               (ECB SDMX, 1y/5y/10y spot rates, daily, AAA issuers)
- R/fetch_eurostat_teimf050.R -> data-apis/eurostat_teimf050_latest.{parquet,meta.json}
                               (Eurostat teimf050, 10y gov bond yields, monthly, by country)
- R/fetch_ecb_mrr.R          -> data-apis/ecb_mrr_latest.{parquet,meta.json}
                               (ECB main refinancing rate, event-driven, since 1998)
- R/fetch_ecb_hicp.R         -> data-apis/ecb_hicp_latest.{parquet,meta.json}
                               (ECB HICP year-on-year inflation, monthly, since 1998)
- R/freshness.R: new freshness_badge_combined(list_of_metas) helper —
  one badge per page based on the oldest fetched_at, with graceful
  fallback when meta is malformed.
- trackers/eu-finance.qmd: 3-section tracker page with combined freshness
  badge, ggforce::facet_zoom on two of the three plots, interactive
  ggiraph layers, inline computed stats (current max/min country, EA vs
  non-EA averages over last 12 months).
- _quarto.yml: navbar now has a Trackers dropdown.

Renv:
- Pins 5 new top-level packages: eurostat, ggforce, dplyr, lubridate,
  stringr (the last three were transitive — making them explicit so the
  lockfile makes intent visible).

Initial snapshots are committed alongside the fetchers so the first CI
render has data to read (otherwise read_with_freshness() would stop()).

Spec:  docs/specs/2026-05-24-eufinance-port-design.md
Plan:  docs/plans/2026-05-24-eufinance-port.md

AGENTS.md migration map updated to mark eu-finance row done.

Local smoke render verified: _site/trackers/eu-finance.html builds with
all 3 sections, combined freshness badge (callout-tip, fresh data),
ggiraph SVGs in all 4 hexes of the Okabe-Ito palette, citation link to
michalovadek.github.io/euverse/trackers/eu-finance.html.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
```

- [ ] **Step 5: Commit and delete the temp file**

```powershell
git commit -F .git_commit_msg.tmp 2>&1 | Out-String
Remove-Item .git_commit_msg.tmp
git log --oneline -3
```

Expected: commit succeeds; `git log --oneline -3` shows the new commit at HEAD followed by `50aeb80` (the migration commit) and `c29ddef` (initial).

- [ ] **Step 6: Push**

```powershell
git push origin main 2>&1 | Out-String
```

Expected: push succeeds, ends with `50aeb80..<new-sha>  main -> main`.

If the push is rejected (someone pushed to euverse from elsewhere in the meantime), do NOT `--force`. Surface BLOCKED to the controller.

- [ ] **Step 7: CI observation URL**

After push, the next workflow run can be watched at:

```
https://github.com/michalovadek/euverse/actions
```

Expected behaviour on the first run:
- `render` job runs in 2–4 min (cached renv from prior commit + 5 new packages installed cold).
- Each of the 4 new `R/fetch_*.R` runs sequentially during the "Fetch nightly data" step. All should succeed against the live ECB / Eurostat APIs; if any one fails, it emits a `::warning::` annotation but the build continues and the page renders from the snapshots committed in this push.
- `deploy` job stays SKIPPED (`if: false` gate, intentional until go-live).
- Live site (still served from `michalovadek.github.io` repo's static HTML) is UNCHANGED.

If `render` fails:
- Most likely culprit: `ggforce` needs a Linux system lib (e.g. `libudunits2-dev`) not present on the ubuntu-latest image. Mitigation: insert `r-lib/actions/setup-r-dependencies@v2` with `packages: any::renv` before `setup-renv` in `.github/workflows/render.yml`. Commit + push that one-line change separately.
- Second-likeliest: `eurostat::get_eurostat` rate-limited or upstream column rename. The page still renders from the committed snapshot; only the `::warning::` shows in the Actions log.

---

## Done criteria

This phase is complete when ALL of:
- New commit exists on local `main` and on `origin/main`.
- The most recent GH Actions "Render and deploy" run shows `render: success`, `deploy: skipped`.
- `_site/trackers/eu-finance.html` rendered locally shows all 3 sections (yield curve, country yields, rates + inflation) with real data and a fresh `callout-tip` freshness badge.
- `read_with_freshness()` calls in `trackers/eu-finance.qmd` resolve to all 4 snapshots and the combined badge labels the oldest of them.
- AGENTS.md §9 has `✅` next to the eu-finance row.
- `_site/`, `.quarto/`, `_freeze/`, `renv/library/`, `.venv/` are absent from `git ls-files`.

When all six bullets above are true, the next phase begins: porting the second tracker. Per the spec's recommendation order, next is `eu-vetoes` (the only manually-curated tracker — exercises the `data-manual/` path with the existing `ms-vetoes.csv` already in this repo); after that the heavier eurlex-driven `eu-court` and `eu-law` ports.
